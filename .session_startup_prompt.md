# Session Startup Prompt -- TVB-35

**Previous Session:** TVB-34 (2026-09-05/06): deep-dive review folded, round-3
package built, ROUND 3 LIVE on the VPS (effective open 2026-09-06 19:52:53Z)
**Current Branch:** `main` (this repo); executor `main` 5cd2b0d = deployed
9f39ba9 + README status (LIVE; tmux `executor`)
**Commit state:** pushed (both repos)
**Plan Mode:** OFF for monitoring; ON for any rule change (prereg first)
**Prior-session review:** REQUESTED -- next session checks docs/reviews/tvb34-codex-audit.md
(TVB-31/32/33 audits still unreturned)

---

## READ FIRST

The executor is LIVE (round 3: fee floor + weekly dot on gate and flip + four
seats + $1 risk on $202 + the deep-dive repairs; extended hours shadow-only).
Never touch data/KILL_FLAT or restart the loop without the user. Round 2 is the
control; the round-3 ledger opens at the 19:52:53Z startup row (the 16:26-19:52
window had no possible entries: the venue had pruned the old agent). The single
most important next move is monitoring, then the round-3 close-out replay.

## NEXT (in order)

1. Monitor (read-only over SSH on the user's word): `liq_inside_stop` must never
   fire; `fee_rate_unavailable` never appears; the first entry receipt should
   carry `lev` and `liq_px_venue`; every decision row carries stack /
   stack_60dwm / score / rank / session. Report in trader terms with a
   bar-by-bar walkthrough of any trade.
2. Archive HANDOFF (over 1,500 lines) on the user's word.
3. Round-3 close-out replay: add the LedgerSpec, read shadows journaled-first,
   replay against round 2; then the score-picker arm receipt.
4. Scanner release: the three STRAT rulings + PR-B / PR-A.
5. Carried: July A0b anchor under feasible fills; prospective halfway
   generator; slippage model; TVB-18 repairs; month-end fresh-window regen.

## Open questions carried from the charter (Section 8)

- Which continuity stack is "STRAT" (four intraday dots + week now live vs the
  sheet's 60/D/W/M): both verdicts journaled; a future arm, not a ruling.
- The reviewer's no-progress time exit: parked until more tape.

## Standing constraints

- Never hardcode the VPS IP (host from the shell history, `<VPS_IP>` in docs);
  VPS SSH only with explicit user confirmation; the master wallet address lives
  only in the private executor repo; `scripts/secret_scan.py` before every push.
- After any full withdrawal the venue prunes the API wallet: re-approve the
  agent BEFORE re-funding; check `extraAgents` before go-live.
- Every rule change = a dated ruling + prereg BEFORE code; single-change arms.

## Reference

- docs/HANDOFF.md TVB-34 entry + TVB-33 section 7 (the review synthesis)
- docs/reviews/deep-dive-2026-09-05-astra.md; docs/ARM_LEDGER.md (round-3 cards)
- C:\Strat_Trading_Bot\hip3-executor README (amendments 2026-09-06b/c, STATUS 16:26Z)
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first)
- CLAUDE.md (STRICT COMPLIANCE)
