# Session Startup Prompt -- TVB-12

**Previous Session:** TVB-11 (2026-07-10..15; champion search executed 07-14/15)
**Current Branch:** `main`
**Commit state:** pushed to `origin` (PUBLIC) through the session-end commits
**Plan Mode:** OFF for mechanical work. Any new pre-registration (weekend
ablation, Stage C scalp satellite) is decide-WITH-user: draft -> explicit
approval -> run.
**Prior-session review:** REQUESTED -- check `docs/reviews/tvb11-codex-audit.md`
(via `docs/reviews/REVIEW_REQUEST.md`, which pins the TVB-11 range). tvb8/tvb9
audits remain unreturned (standing note).

---

## READ FIRST

TVB-11 completed the CHAMPION SEARCH (pre-reg + results:
`docs/experiments/tvb11_champion_prereg.md`, Section 12): the labeled
in-sample ceiling exists (+249% perp 15m / +766% mirror 60m), the
generalization gap is measured (ceiling -> +3..6% median across 7 roster
perps), and three minimal winner indicators are deployed. At session end the
user set the NEXT ARC (verbatim intent below -- this is the session's main
work, a DESIGN-WITH-USER session): combine the three winners into ONE
indicator with a live REGIME LABEL, make it viewable on higher-TF charts,
and expand the date range beyond TV's rolling data floor.

## NEXT (in order)
1. Review fold-in FIRST: the user is running the Codex review themselves and
   will PASTE the audit output at session start (or it lands in
   docs/reviews/tvb11-codex-audit.md). Read it critically, write the
   synthesis into HANDOFF, act on CRITICAL/HIGH before new work.
2. COMBINED WINNER INDICATOR (design WITH user, then build):
   (a) One script holding all three winner configs (Champion MU15,
       Generalizer, Slow60) plus the un-shipped 5m-window SHORT champion
       (slow3/state/short/BF-ratchet_c a15/x60, +12.9% on the chop window)
       as toggle boxes -- shorts ARE in scope now; the user accepts chop
       losses as irreducible.
   (b) A live REGIME LABEL: classify current conditions as closest to
       "longs rewarded" (trend up: flip+long configs printed), "shorts/chop
       rewarded" (state-exit shorts printed), or neutral -- explicitly
       approximate, not predictive. Candidate structural inputs from the
       TVB-11 data: gate-agreement persistence, flip-line cross cadence
       (TVB-10 diagnostic), BF-line touch frequency, D/W/M alignment
       fraction. DESIGN DECISION with user: label from a-priori structural
       features, NOT tuned on the sample (charter S7 applies again --
       the champion license does NOT extend to the regime classifier).
   (c) HTF VIEWABILITY: the signal logic runs on its optimal clocks (15m
       arming etc.) but the user wants to VIEW signals on hourly/daily
       charts for context. Design tension to surface honestly:
       chart-TF-locked reconstruction (current scripts) cannot run above
       arm TF; options = request.security_lower_tf (limited LTF history
       depth, intrabar approximation), security() of the gate states only
       (signals approximate), or multi-pane layout guidance. Pick WITH
       user; do not fake LTF fidelity on an HTF chart (same never-fake
       rule as intrabar arming).
3. DATE-RANGE EXPANSION (the 5m-window limitation, user priority):
   TV's rolling floor gives ~10 weeks of 5m -- verdicts on 5m cells are
   window-limited and the floor slides daily (anchors already went stale
   once). Path to discuss: VBT Pro breadth engine (exists, gate-proven
   8/8 vs TV) + Hyperliquid API pulls. Constraints to state: HL serves
   only ~5000 recent candles/interval (evidence time-perishable -- TVB-6),
   so (i) START ARCHIVING 5m/15m pulls NOW to accrete history forward,
   (ii) the BF engine exists only in Pine -- a VBT port of BF pivots/lines
   is new, gate-able work; (iii) user is complexity-wary: scope smallest
   useful step first (archival cron + long/short cells in VBT on existing
   60m committed pulls, BF-off). Also revisit the perp-vs-mirror window
   confound with equal-calendar windows once data allows.
4. Weekend/low-liquidity ablation pre-reg (memory:
   project-weekend-liquidity-protocol): pause Fri 6pm -> Sun 6pm ET
   (America/New_York-anchored, DST-aware), entry-pause vs force-flat vs
   control; own small pre-reg, decide WITH user.
5. OPTIONAL Stage C scalp satellite (pre-reg Section 5, deferred);
   deferred carries: SIL1! data entitlement question, VBT-port governor
   parity re-check (profit() is NET), Korea-cluster tests (FYI).

## Open questions carried from the charter (Section 8)
- Entry arming close vs intrabar: still open; companion v5 + Winner
  indicators are the live instruments.
- Price-stop vs state-stop by horizon: Stage C would test the price-stop
  side; still open.
- Exit-mode/governor/regime values are REGIME-SHAPED, not absolute (third
  confirmation in TVB-11 A2) -- any future "keep" rule must name its regime.

## Operational notes (hard-won this session)
- TV Desktop on CDP 9222 (Store app; direct exe launch with the flag; kill
  first, bare relaunch does not attach). Champion strategy artifact:
  "TVB-EXP Champion [TVB-11]" (USER;1a68d411...), UNCHANGED and anchored;
  the conditional-guard fix exists ONLY in the repo (pine/tvb_exp_champion
  .pine) -- TV refused re-saves after editor reopen (trap memory updated);
  redeploys need a fresh Make-a-copy.
- Collector: scripts/tvb11_champion_collect.mjs (echo-first/engine-fallback
  settle gate, load-to-floor history, RTH-mirror session forcing,
  checkpoint/resume). Cell lists + all run JSONLs committed under
  docs/experiments/.
- NASDAQ:MU chart session preference restored to '24h' (the user's setting).
- TV per-symbol session prefs can be 24/5 overnight feeds -- ALWAYS verify
  subsession on RTH mirrors (C3 gate caught it live).

## Reference
- docs/experiments/tvb11_champion_prereg.md -- pre-reg + amendments + RESULTS
  (Section 12) = the record
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first); CLAUDE.md
  (STRICT COMPLIANCE); docs/HANDOFF.md (TVB-11 entry at top)
- Memory: project-champion-baseline-request (EXECUTED), feedback-results-
  plain-language, feedback-underlying-rth-mirror-rule, project-weekend-
  liquidity-protocol, reference-korea-session-structure,
  project-live-micro-deployment-plan, tv-mcp-pine-tab-binding-trap (new
  re-save facet)
