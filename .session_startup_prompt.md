# Session Startup Prompt -- TVB-11

**Previous Session:** TVB-10 (2026-07-08)
**Current Branch:** `main`
**Commit state:** TVB-10 committed + PUSHED to `origin` (PUBLIC) through the
session-end commits. No sibling-repo changes this session.
**Plan Mode:** OFF for mechanical work. The entry-arming ablation is a
decide-WITH-user step (draft pre-registration -> explicit approval -> run).
**Prior-session review:** REQUESTED -- check `docs/reviews/tvb10-codex-audit.md`
plus the still-unreturned tvb8/tvb9 audits at start (via
`docs/reviews/REVIEW_REQUEST.md`, which now names the TVB-10 range).
**Model note:** TVB-10 ended with ~4% weekly Fable budget; this session may be
on a different model. Everything needed is in this file + HANDOFF.

---

## READ FIRST

TVB-10 ran the exit-symmetry ablation (state-stop vs flip-stop, 288 runs) and
shipped the companion v3->v4.1. But the session ended with a USER LIVE FINDING
that overrides the queue -- notate-first directive, user's words paraphrased:

**"There is something not quite dialed in on the strategy."** Evidence: a
tracked would-be bad losing position on EWY (2026-07-08, latest companion
logic) plus a full day of live watching. Diagnosis (user's, and it is
well-founded): **the strategy arms entries/exits only at strategy-TF CLOSE,
while the original TFO Timeframe Continuity indicator (and STRAT doctrine,
charter 2U-timing) flags alignment the INSTANT close-vs-period-open crosses,
intrabar.** The user's CL reference config was 1M/1W/1D/4H/1H stacked --
five TFs, NO regime layer, intrabar alignment entry -- and it churns far
less than the strategy did all day.

The "not dialed in" decomposes into THREE axes -- only one is tested:
1. **Exit mode** (state vs flip) -- DONE, TVB-10: a regime-shape bet, not a fix.
2. **Entry arming** (close-armed vs intrabar-armed) -- UNTESTED. This was the
   deferred "net-arming ablation"; the EWY loss promotes it to next pre-reg.
   Design tension to surface honestly: the trigger is a monotonic level break
   (intrabar-safe) but the gate is a REVERSIBLE threshold (close vs period
   open can cross back intrabar). Intrabar arming on 1h bars CANNOT be
   simulated faithfully from 1h data alone -- it needs committed LTF pulls
   (HL 15m/5m) or explicit best/worst-case bracketing assumptions, stated in
   the pre-reg. Do NOT fake it with bar extremes and call it tested.
3. **Stack depth / composition** (user's 5-TF M/W/D/4H/1H no-regime vs our
   ctrlA M/W/D/60 + regime) -- UNSWEPT. Deeper stacks make full alignment
   rarer -> longer regimes -> less churn by construction. A mirror cell
   (M/W/D/240/60, no regime layer) belongs in the same pre-reg.

## NEXT (in order)
1. **Review fold-in**: tvb10-codex-audit.md (+ tvb8/tvb9 if returned).
2. **Entry-arming + stack-depth ablation pre-registration (decide WITH
   user)**: cells and data plan per the axes above; expectations BEFORE runs
   (prediction: intrabar arming enters earlier at better prices in bursts
   but re-introduces flicker churn around the flip line; the 5-TF no-regime
   stack cuts cadence but loses the stand-aside containment TVB-9 proved).
   Companion gets an arming-mode surface for live A/B per the standing
   companion-sync rule (memory: feedback-companion-sync-cadence).
3. **Long-only ablation pre-registration** -- motivation now
   exit-mode-robust (MU shorts worse under BOTH exit modes). Can ride along
   or follow.
4. **Deferred carries**: weekday-RTH L2 re-sample (14:30-20:00Z window);
   Pine governor-comment fix with next STRATEGY-slot deployment (+ pv20
   prefix regression); CLUSDC.P out-of-family check (own mini pre-reg,
   fresh HL fetch + commit); HTF flip cells (MWD_on240/onD x exit_mode).

## Open questions carried from the charter (Section 8)
- Entry timing: close-armed vs intrabar-armed is now an OPEN charter-level
  question (charter 2U-timing says intrabar; the engine arms at close for
  gate-fidelity reasons). Resolve by ablation, not by argument.
- Price-stop vs state-stop by horizon -- still open.
- Vol/trend-conditional grey rule -- PARKED (dead-band D1 was weak, 22/36).
- Re-entry governor: KEPT for state-stop; structurally INERT under flip.

## Operational notes (hard-won)
- **TV Desktop RUNNING** (CDP 9222). Companion lives in script "TFC Companion
  [TVB-10]" (v4.1, id USER;b3f894590cdc4e4aaa4add3c74209c7b). STRATEGY slot
  untouched all session (pineVersion 20 anchor intact). Housekeeping for
  user: TV library script "Price Level Alerts (Interval Bands)" contains
  companion v2 source (pre-existing TVB-9 damage; original likely lost).
- VBT Pro v2026.6.27 in .venv only; `uv sync` STRIPS it (`uv run --no-sync`).
- HL pulls: committed files are the evidence (cap slides daily); runners are
  idempotent from committed pulls; new symbols/TFs need fetch + commit.
- Equivalence gate tests/test_tfc_equivalence_gate.py must stay 8/8 green
  through ANY simulator change (exit_mode passed it: state arm reproduced
  TVB-9 exactly).

## Reference
- `docs/TVB2_control_AB_rerun.md` -- TVB-10 sections (pre-reg, C2 amendment,
  results scorecard, companion deployments) = the record
- `docs/ATLAS_Timeframe_Continuity_Charter.md` (Section 0 first); `CLAUDE.md`
  (STRICT COMPLIANCE); `docs/HANDOFF.md` (TVB-10 entry at top)
- Memory: `tvb10-exit-symmetry-regime-bet`, `project-entry-arming-fork`,
  `feedback-companion-sync-cadence`, `feedback-pine-script-ux-style`
