# Session Startup Prompt -- TVB-7

**Previous Session:** TVB-6 (2026-07-03)
**Current Branch:** `main`
**Commit state:** TVB-6 committed + PUSHED to `origin` (PUBLIC) through the session-end
commit. Sibling repo tradingview-mcp-jackson has local commit `27757bc` (Monaco finder
fix) -- local transport only, picked up by the MCP on next restart.
**Plan Mode:** ON for the VBT breadth-engine port design (architecture fork); OFF for
the OKX governor re-verification runs (mechanical re-reads).
**Prior-session review:** REQUESTED -- check `docs/reviews/tvb6-codex-audit.md` at start
(via `docs/reviews/REVIEW_REQUEST.md`, the reviewer pointer).

---

## READ FIRST
TVB-6 resolved the venue gap (xyz TV backfill VERIFIED = genuine HL venue data; the gap
then DECOMPOSED: ~half was a tick-size modeling artifact -- OKX mintick 0.01 vs xyz
0.001 under the "1 tick/fill" convention -- and ~half real venue texture); xyz
MSTRUSDC.P is now the PRIMARY MSTR chart (OKX = standing cross-venue sign check).
Short-leg 1x-cash solvency CLEARED (worst MAE 8.11% vs +90.5% liq). The re-entry
governor (zero-parameter level ratchet, charter S8) was designed with the user, v1
exposed a reset-starvation interaction under stand_aside (finding, recorded), v2
(alignment reset) was KEPT by pre-registered rule: R1E1+gov2 = +71.80 @0.0125 s1 /
+45.71 s10 vs ungoverned +59.96/+34.76. ALL of this is one instrument, one 7-month
path (ctrlB was underwater 204/213 days; even R1E1+gov2 124 days) -- single-sample
fragility is now the BINDING CONSTRAINT, hence the user's new direction: breadth.

## NEXT (in order)
1. **Cross-instrument/venue re-verification of governor v2.** Same seven governed
   cells on OKX:MSTRUSDT.P (15m floor Feb-25; 60m deeper), plus a BTC 60m E3
   spot-check. Question: is the gov2 lift structural or xyz-path-local? Keep-rule
   language stays provisional until this passes.
2. **VBT Pro breadth-engine port -- DESIGN with user (plan mode).** TV = fidelity
   anchor; VBT = breadth (many symbols, longer history, incl. regular equities as a
   DIFFERENT regime family -- RTH gaps change trigger/stop semantics, charter S2).
   HARD GATE before any VBT number enters the record: engine-equivalence -- VBT must
   reproduce the TV xyz cells trade-for-trade on the committed venue-verified bars
   (`analysis/reference/tvb6_tv_xyzMSTR_15m.json` + enriched tvb6 dumps with
   et/xt/ep/xp per trade make this provable). Lives in/uses vectorbt-workspace infra
   (5-step VBT workflow MANDATORY).
3. **Cost realism completion (carries):** live xyz L2 book sampling at ~90-contract
   size (turn the slippage BAND into a venue-realistic point estimate); perp FUNDING
   modeling (scales with notional; the one cost leverage does not shrink).

## Open questions carried from the charter (Section 8)
- Re-entry governor: ANSWERED-PROVISIONAL (v2 alignment-reset ratchet KEPT by
  pre-registered rule on xyz; cross-instrument re-verification = TVB-7 job 1;
  gov_mode input default stays `off` = regression anchor).
- Grey handling: RATIFIED (stand_aside). Regime-speed: monotone-destructive (TVB-5);
  slower-than-M/W/D regime still an untested a-priori candidate.
- Price-stop vs state-stop by horizon -- still open (bar-magnifier re-check required
  if a price-stop variant is built).
- Vol/trend-conditional grey rule -- still PARKED (charter S6 bar unmet).

## Operational notes (hard-won)
- TV launch (Store app): kill any running TradingView, then
  `Start-Process (Join-Path (Get-AppxPackage TradingView.Desktop).InstallLocation
  'TradingView.exe') -ArgumentList '--remote-debugging-port=9222'`.
- Pine editor: TV 2026-07 renders a fiber-less DECOY `.monaco-editor.pine-editor-monaco`
  node -- jackson's finder FIXED (`27757bc`, multi-candidate walk) but the MCP loads it
  only after restart; until then use the bridge scripts in the TVB-6 scratchpad pattern
  (set source via CDP + click the circular-arrows update icon next to the script name;
  the old "Add to chart" text button is GONE from the header).
- Strategy slot "TFC Baseline" at **pineVersion 20.0** (governor v2). ID MAP (changed
  this session, +1 shift): exec in_2..in_13 pairs, in_16 reg_mode, in_18/20/22 regime
  TFs, in_23 grey=50, in_24 reg_exit, **in_25 gov_mode ('off'|'ratchet')**, in_33
  commission, in_34 slippage, in_35/36 margins 0/0, in_42 magnifier. Entity `SCn1V9`
  until TV restarts (then re-derive; scratchpad tv_inputs_map.mjs pattern).
- `node scripts/tv_probe.mjs history` before EVERY read; dumps via
  `node scripts/tv_dump.mjs <out>` (TVB-6 format: et/xt/ep/xp/dir/pp/pv/q/ddp/rnp +
  fail-loud assert block; exit 2 = closed/open split untrustworthy).
- Chart resting state: HIP3XYZ:MSTRUSDC.P 15m, ctrlB defaults, reg off, gov off,
  fee 0.0125, slippage 1 -- regression anchor +8.0253%/4,309 closed (Jul-3 23:15Z tail).
- xyz convention: report churn-config numbers at s1 AND s10 ($0.01/fill equalizer);
  never s1 alone.

## Reference
- `docs/TVB2_control_AB_rerun.md` (canonical datasheet; TVB-6 sections: backfill
  verification, mechanism peek + knife-edge, xyz-native rows, MAE/solvency,
  slippage band + tick-size decomposition, governor v1/v2 pre-registrations + results)
- `docs/ATLAS_Timeframe_Continuity_Charter.md` (Section 0 first); `CLAUDE.md` (STRICT
  COMPLIANCE); `docs/HANDOFF.md` (TVB-6 entry at top)
- Memory: `project-vbt-breadth-engine`, `user-leverage-philosophy`,
  `project-one-layer-screener-attach`, `tvb5-sweep-containment-venue-gap` (updated)
