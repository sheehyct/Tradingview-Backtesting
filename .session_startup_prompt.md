# Session Startup Prompt -- TVB-18

**Previous Session:** TVB-17 (2026-07-27; context-engineering audit of standing
context -- CLAUDE.md trimmed 44%, dead Stop test gate fixed, TVB-11 finding F7
executed; external review SKIPPED)
**Current Branch:** `main`
**Commit state:** pushed to `origin` (PUBLIC)
**Plan Mode:** OFF for the archive/replay catch-up (mechanical); ON for the
adverse-runner exit design (decide-WITH-user, STOP-and-ASK zone).
**Prior-session review:** TVB-17 skipped (context session). TVB-15 review is
STILL PENDING -- check `docs/reviews/tvb15-codex-audit.md` at startup.

---

## READ FIRST

**Paper week 1 has CLOSED** (window was 2026-07-20 00:00 -> 2026-07-27 00:00
UTC). The record is current only through 07-22 02:05Z. Nothing has been lost --
the HL 5m floor is ~17 days, so the 07-20 window start survives until roughly
2026-08-06 -- but that headroom is finite and the week-end pass is now the
overdue main event.

TVB-17 did NOT do paper work; it was a workspace/context session. Priorities 1
and 2 below are TVB-17's original priorities carried forward unchanged.

Note on CLAUDE.md: it is 44% shorter than you last saw it. Nothing classified as
a domain invariant or a gotcha was removed -- the cuts were self-restatement,
spine duplication, and procedure that `/session-start` performs. Six real traps
were promoted INTO it out of auto-memory. If something feels missing, check
`7ebc955` before re-adding it.

## NEXT (in order)

1. ARCHIVE CATCH-UP then WEEK-END PASS. `uv run python -m analysis.paper.archive
   --roster analysis/paper/roster_week1.json`, then `... -m
   analysis.paper.replay --roster ...` to close out the full week. Then the
   week-end pass per `docs/experiments/tvb15_paper_week1_protocol.md`: TV-vs-twin
   parity on DRAM + 2 roster names, exit-class/give-back report (state the
   heat-conditioning of the sample).
2. DESIGN THE ADVERSE-RUNNER FIX **with the user** -- the week's headline
   finding, config-invariant: three shorts (NBIS -22%, DRAM -13%, MRVL -11%)
   rode ~49h with no exit because the flip backstop needs FULL D/W/M and price
   ran in open air with no line in the path. Options on the table: flip
   full-vs-partial D/W/M granularity, or a structural open-air stop.
   STOP-and-ASK, design before code.
3. `/pre-commit` Check 2 still carries the OLD `lookahead_off` rule -- the
   permission classifier blocked that edit in TVB-17. Until fixed it reports a
   false FAIL on 5 Pine files. One-line fix; see the TVB-17 HANDOFF entry.
4. TVB-15 external review: fold in `docs/reviews/tvb15-codex-audit.md` if
   returned. If still pending, note and proceed.
5. Week-2 protocol (candidate = full-universe twin; roster = attention subset;
   decide pre-registered), then the deferred audit items (last-only supersede,
   fixture assertions, min_sep holdout-freeze).

## Deferred from the TVB-17 audit (all optional, none urgent)

- `safety_guard.py`: `RE_DOCKER_PROD` compiled but never called; SSH/SCP and
  VPS-IP rules unenforced; `PROJECT_ROOTS` omits this repo and all `hip3-*`.
- `checkpoint_review_prompt.py` is orphaned (wired in no settings file).
- `.pine` files have no automated check (`post_edit_lint.py` is Python-only).
- Proposed-but-unbuilt gates: a pytest for the `request.security` idiom, and an
  ASCII/emoji check.
- Rotate the plaintext `OPENAI_API_KEY` in `~/.claude/settings.json`.

## Open questions carried from the charter (Section 8)

- Flip-backstop granularity (full vs partial D+W) -- the CORE of priority 2.
- Twin fidelity deltas: flat-seed entry timing (materially inflated the stuck
  shorts' severity); live-config mismatch RESOLVED as framing (frozen twin =
  shipped control; user's 1H/12h-off = an owned post-ship variant).
- Cross-pool duplicate lines (no dedup); consumption-on-any-touch review;
  same-bar entry+touch edge (documented, accepted).
- Week-2 roster contamination fix (full-universe twin vs heat-conditioned
  labeling).

## Reference

- `docs/experiments/tvb15_paper_week1_protocol.md` (the week's contract)
- `docs/HANDOFF.md` TVB-17 entry (audit) and TVB-16 entry (the paper arc)
- `analysis/paper/compare_config.py` (control-vs-variant ablation harness)
- `docs/ATLAS_Timeframe_Continuity_Charter.md` (Section 0 first)
- `CLAUDE.md`
