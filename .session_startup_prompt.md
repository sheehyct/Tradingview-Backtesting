# Session Startup Prompt -- TVB-31

**Previous Session:** TVB-30 (2026-08-28 -- TVB-29 BLOCK audit folded
same-day across all three repos; live run deferred to Monday)
**Current Branch:** `main`
**Commit state:** pushed (concrete shas pinned in docs/reviews/REVIEW_REQUEST.md)
**Plan Mode:** OFF for the live-ops checklist; ON only if the HyPaper
discussion turns into a design/adoption session
**Prior-session review:** REQUESTED -- next session checks
docs/reviews/tvb30-codex-audit.md

---

## READ FIRST

TVB-30 folded the TVB-29 audit (verdict BLOCK, 0C/4H/6M/2L -- all 12
findings reproduced first, 16/16 probes -> 0/16, zero disputes; three
new dated rulings 2026-08-28: reach fail-closed, risk receipt+warn
1.5x, stop restore-else-flatten). Executor a23ac43 pushed (64 tests),
scanner fold = PR #6 (NEEDS USER MERGE), this repo pushed. The round-2
live run was deferred by the user to MONDAY 2026-08-31: scheduling,
plus Friday late-day OPEX pinning (MMs pin underlyings into expiry,
propagating to the HL oracle -- recorded as a second a-priori dead
zone). Monday = short discussion FIRST, then the go-live checklist.

## NEXT (in order)

1. DISCUSSION (user-flagged): HyPaper adoption
   (github.com/GigabrainGG/HyPaper -- drop-in HL API paper twin;
   assessment in the TVB-30 HANDOFF entry). User goal: run MULTIPLE
   PARALLEL STRATEGIES continuously (per-wallet accounts, several
   strategies on the same ticker) and compare results. Decision shape:
   a spike first -- (a) xyz builder-dex support (the exact seam TVB-30
   fixed), (b) Python-SDK compatibility (SDK signs; HyPaper wants
   unsigned JSON + wallet field), (c) mid-cross fill realism vs our
   PaperBroker. Comparison discipline: parallel arms = generating
   data; promotion rules unchanged (ablation, not tournament).
2. Scanner: PR #6 MERGED (user, 2026-08-30 @ 6a7a53c -- both cont
   targets and the parity preflight are in main); remaining =
   `railway up --detach` + post-deploy verification (/health, payload
   probe showing a cont signal WITH a target, smoke). Confirm whether
   any deploy has happened since PR #1 merged -- cont targets do not
   exist live until one has.
3. Executor deploy to the ATLAS VPS via deploy/deploy_from_dev.ps1
   (a23ac43 or later; records DEPLOYED_SHA, now consumed by startup
   provenance). SSH touches production -- explicit user go per spine
   rules.
4. VPS dry-run: verify refusal reasons incl the NEW reach_unavailable
   (+ counter_drift crypto-only, target_beyond_reach,
   underlying_closed) and cont targets resting in paper brackets; run
   the KILL_FLAT drill flat (clean receipt + failed_closes field +
   halt). Then `rm data/KILL_FLAT` DELIBERATELY, set live env, start.
5. Supervised probes = first live trades: one bracket with verified
   resting stop + risk_usd_booked/stop_venue in the entry row; equity
   formula vs venue UI with exactly ONE isolated position (STILL
   UNVERIFIED -- gate item 6's open half); the FIRST xyz fill (confirm
   dex-scoped coin naming AND whether user_fills carries builder-dex
   fills -- it has NO dex param in SDK 0.24.0).
6. Run to day end; KILL_FLAT on the user's word; zero/zero receipt;
   ledger -> runs/2026-08-31_monday/.
7. Carried: month-end fresh-window regen ~Sep 1 (re-pins collision
   census P2 14/9 PX 21/10 + receipts + fee_sides + net algebra + NEW
   LOW-1 full-precision rollup fields); TVB-30 review fold when
   returned; TV mirror on demand; TVB-18 repairs; jackson set_inputs
   fix; tvb8/tvb9 unreturned.

## Open questions carried from the charter (Section 8)

- Anchor clock + weekend-protocol arms: pre-registered, not run. NEW
  ADDENDUM 2026-08-28: Friday late-day OPEX pinning recorded as a
  second a-priori dead zone (memory: weekend/low-liquidity protocol).
- Equity-side drift reference: deliberately unchosen (future a-priori
  choice); xyz runs unfiltered.
- Exit-design lane: runner profiles past T1 remain a future prereg.

## Reference

- C:\Strat_Trading_Bot\hip3-executor (README = Ruleset v1 + 2026-08-28
  amendments + honest gate STATUS; tests/ = 64)
- hip3-scanner PR #6 (parity preflight) -- merge pending
- docs/reviews/tvb29-codex-audit.md (folded) + REVIEW_REQUEST.md (TVB-30)
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first)
- CLAUDE.md (STRICT COMPLIANCE)
