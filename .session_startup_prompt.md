# Session Startup Prompt -- TVB-17

**Previous Session:** TVB-16 (2026-07-22; ride-along refresh + config ablation
tool + mid-week check-in; external review SKIPPED -- light session)
**Current Branch:** `main`
**Commit state:** pushed to `origin` (PUBLIC) through the session-end commits
**Plan Mode:** OFF for the ride-along refresh (mechanical); ON for the
week-end adverse-runner exit design (decide-WITH-user, STOP-and-ASK zone).
**Prior-session review:** TVB-16 not submitted (light). TVB-15 review is
STILL PENDING -- check docs/reviews/tvb15-codex-audit.md at startup; fold in
critically if it has been returned.

---

## READ FIRST

Week 1 of the paper protocol is RUNNING (window 2026-07-20 00:00 ->
2026-07-27 00:00 UTC; docs/experiments/tvb15_paper_week1_protocol.md is the
contract, now carrying the TVB-16 mid-week check-in). The record is current
through 07-22 02:05Z (deterministic Python twin; recording verified sound --
additive, 24 goldens green, day-1 parity exact). The WEEK'S HEADLINE FINDING
is logged: a config-invariant adverse-runner exit gap -- three shorts (NBIS
-22%, DRAM -13%, MRVL -11%) rode ~49h with no exit because the flip backstop
needs FULL D/W/M and price ran in open air with no line in the path;
IDENTICAL across the 12h/arm ablation. Single most important next move:
keep the cadence (archive+replay by ~07-25, 5m floor slides), then at week
end run the pass + design the adverse-runner fix WITH the user.

## NEXT (in order)

1. RIDE-ALONG REFRESH (<= every 3 days): `uv run python -m
   analysis.paper.archive --roster analysis/paper/roster_week1.json` then
   `... -m analysis.paper.replay --roster ...`; present the scoreboard delta;
   re-run `... -m analysis.paper.compare_config --roster ...` to watch the
   ablation populate; collect user exit-class grades; number any user-vs-twin
   or chart-vs-twin divergence TVB15-Dn in the protocol doc.
2. WEEK END (>= 2026-07-27): the week-end pass per the protocol -- TV-vs-twin
   parity on DRAM + 2 roster names, exit-class/give-back report (state the
   heat-conditioning of the sample), THEN design the HEADLINE adverse-runner
   fix WITH the user (flip full-vs-partial D/W/M granularity, or a structural
   open-air stop) -- STOP-and-ASK, design before code. Then the other
   deferred audit items and the week-2 protocol (candidate = full-universe
   twin; roster = attention subset).
3. TVB-15 external review: fold in docs/reviews/tvb15-codex-audit.md if
   returned (engine port fidelity, v6.1 fix edges, golden methodology). If
   still pending, note and proceed.
4. Scanner thread (hip3-scanner workspace, NOT here): TVB side may start
   consuming tf.{1d,1w,1M}.formingDir as the true gate readout any time
   (already served by /api/state; zero scanner deploy).
5. Standing (budget only, LAST): recurring archive automation (the ~3-day
   cadence is the standing operational risk); bf_comprehension audit fixes;
   the deferred audit items (last-only supersede, fixture assertions, min_sep
   holdout-freeze).

## Open questions carried from the charter (Section 8)

- Flip-backstop granularity (full vs partial D+W) -- now the CORE of the
  week's headline adverse-runner finding; design at week end.
- Twin fidelity deltas: flat-seed entry timing (materially inflated the stuck
  shorts' severity); live-config mismatch is RESOLVED as framing (frozen twin
  = shipped control; user's 1H/12h-off = an owned post-ship variant).
- Cross-pool duplicate lines (no dedup); consumption-on-any-touch review;
  same-bar entry+touch edge (documented, accepted).
- Audit-deferred: last-only supersede search; fixture two-pass remediation;
  min_sep holdout-freeze protocol. Week-2 roster contamination fix
  (full-universe twin vs heat-conditioned labeling; decide pre-registered).

## Reference

- docs/experiments/tvb15_paper_week1_protocol.md (the week's contract +
  TVB-16 mid-week check-in with the ablation table)
- docs/HANDOFF.md TVB-16 entry (top) -- the full check-in arc
- analysis/paper/compare_config.py (control-vs-variant ablation harness)
- docs/reviews/tvb14-codex-audit.md (ADDRESSED) + tests/test_paper_engine.py
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first)
- CLAUDE.md (STRICT COMPLIANCE)
