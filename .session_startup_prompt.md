# Session Startup Prompt -- TVB-21

**Previous Session:** TVB-20 (2026-08-07/08)
**Current Branch:** `main`
**Commit state:** pushed (session range `bef6dae^..` -- concrete head pinned in docs/reviews/REVIEW_REQUEST.md)
**Plan Mode:** ON -- this is THE design session; nothing gets coded before the design is approved
**Prior-session review:** REQUESTED -- next session checks docs/reviews/tvb20-codex-audit.md

---

## READ FIRST

The v6.1 CONTROL strategy() is built, parity-gated PASS (full-span, three
symbols, zero mismatches, break/flip float-exact), and left MOUNTED with the
Strategy Tester open on the TVB18-parity layout -- the A/B measuring stick
exists. The single most important next move is the DESIGN SESSION: turn
`docs/experiments/tvb20_design_session_seed.md` (the user's rule-base +
layering-arc alignment) plus the standing exit-design scope into
pre-registered arms. Load the strat-methodology skill before any trigger/
stop/target mechanics; every ambiguity is STOP-and-ASK.

## NEXT (in order)

1. DESIGN SESSION (plan mode ON): exit redesign + Magnitude+Targets layering
   in ONE conversation. Inputs: the seed doc (BF-proximity exhaustion VETO
   ~1%/ATR-scaled; T1-always vs 2-3-target arms; ~2% ATR-scaled
   continuity-flip chop veto; 1D governing BF; "how complex do we have to
   make this?" as the null), the TVB-18/19 exit scope (flip coupling/
   UNCOUPLING, gate-vs-scenario flip, open-air stop, targets+BF overlay),
   Tier A's exit costs (brk ~47pp / flip ~35pp median in-window) vs the
   30-40% MAE tail, the week-1 adverse-runner gap, and the anchor-clock
   census (MATERIAL -- clock is a design variable). Ask the user for their
   live-traded M+T setup list at session start if not yet supplied.
   Output: a pre-registration doc, committed BEFORE any runner/variant code.
2. Greenlit repairs bundle (TVB-18): F2 roster receipts + fail-closed, F3
   5m-lifecycle warm-up regression, F4 eviction telemetry split,
   freeze-boundary invariant, SKHX tv_symbol/mintick backfill (future
   rosters only).
3. Tier B pre-registration (design-gated -- only after 1 resolves what the
   arms ARE).

## Open questions carried from the charter (Section 8)

- Anchor clock (UTC vs RTH) is a live design-session variable (TVB-19 census
  MATERIAL: 31% gate disagreement, day-leg structural).
- Weekend/low-liquidity protocol arms (pause vs force-flat vs control) still
  pre-registered, not run.
- Charter S3.1 is now HYPOTHESIS UNDER ABLATION (TVB-20 amendment): the M+T
  layer block vs continuity-only control is the test that resolves it.

## Reference

- docs/experiments/tvb20_design_session_seed.md (the design session's input)
- docs/experiments/tvb20_control_port_parity.md (control conventions; the
  entry/bf close-fill residual and why A/B stays within TV)
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first; S3.1/S5
  amendments 2026-08-08)
- CLAUDE.md (STRICT COMPLIANCE; "Ablation, not tournament" reword 2026-08-08)
- Gotcha reminders for any TV work: TV Desktop must be launched with
  --remote-debugging-port=9222 (kill-first if already running); layout
  switching lands via the Open layout... UI dialog + tab_switch, not the
  internal API; Make-a-copy flow for any new script.
