# Session Startup Prompt -- TVB-3

**Previous Session:** TVB-2 (2026-06-30)
**Current Branch:** `main`
**Commit state:** TVB-2 committed + PUSHED to `origin` (PUBLIC) after secret-scan. Reader fix
committed in `tradingview-mcp-jackson` (SEPARATE repo, commit `fb2a788` on `main`).
**Plan Mode:** OFF for the re-runs (steps 1-3 are mechanical -- inputs/settings only). ON for any
Pine change (slippage modeling, limit-entry variant, P2b) -- strat-methodology STOP-and-ASK.
**Prior-session review:** REQUESTED -- check `docs/reviews/tvb2-codex-audit.md` at start; fold it in.

---

## READ FIRST
TVB-2 fixed the TV MCP reader (clean JSON now) and resolved the fee saga to GROUND TRUTH: the real
xyz taker rate is ~0.0086-0.0125% (HL `crossed` flag, validated against HL's published 0.0432%
crypto taker), ~10x BELOW the 0.1% the backtest used. That REVERSES "B is dead by fees" -- at real
cost A is a clear winner (~+38-42%) and B is near breakeven (~+5-25%). BUT those are GEOMETRIC-MODEL
estimates, and the binding constraint has MOVED to SLIPPAGE (market/taker fills; 2301 round-trips for
B). Single most important next move: re-run A and B at the REAL fee to replace the model with actual
numbers, THEN model slippage. Fee is no longer the blocker. Full datasheet: `docs/TVB2_control_AB_rerun.md`.

## NEXT (in order)
1. **Re-run A and B at the real taker fee** (`in_23` = 0.0086% modal AND 0.0125% notional-wtd) to
   replace the geometric estimates with actual backtests. Reader returns clean JSON. Control B =
   enable 60/30/15 (in_8/in_10/in_12) + disable M/W/D, chart 15m; Control A = enable M/W/D/60
   (in_2/in_4/in_6/in_8) + disable 30/15, chart 60m. Both 1x, both directions, same window. Strategy
   was entity `35dVw8` (RE-CHECK id via chart_get_state; slot "VIX Spike Alert" / title "Baseline TFC [TVB-1]").
2. **Model slippage** (the new binding constraint) -- asymmetric tick offsets (targets fill better,
   stops worse; user confirms ~symmetric in ticks). Hits B's 2301 market round-trips hardest. Pine = STOP-and-ASK.
3. **Verify intrabar fill fidelity / bar-magnifier** (Codex trap #2) -- matters most where per-trade
   edge is thin (B near breakeven).
4. **P2a / P2b (carried):** verify HTF-open currency (overlay strategy FTFC coloring vs the
   `Timeframe Continuity [TFO]` indicator, now on chart); adopt local period-open recon
   `ta.valuewhen(timeframe.change(tf), open, 0)` + trigger tick-offset + chart-TF<=enabled-TF guard.
5. **Cherry-picked "obviously-profitable window" bug-test** (user's idea; brittle-on-purpose, charter
   S1): pick a clean strong MSTR trend window; if not strongly profitable zero-of-fee, a bug is
   exposed (gate staleness, exit, short side).
6. Only AFTER controls are clean at real cost + slippage: two-layer design (charter 3.3); second
   regime window (TVB-2 was a single ~4mo downtrend -- regime flattery risk).

## Open questions carried from the charter (Section 8)
- Chop handling: size-down vs stand-aside when the regime layer is grey (two-layer build forces it).
- Re-entry governor (cooldown / required new extreme) -- B's 2301 trades.
- Price-stop vs state-stop by holding horizon (still state-stop only).
- NEW: limit-entry (maker) variant -- breakout entry is intrinsically TAKER, so maker is NOT a free
  fee swap; likely moot if the cheap-taker baseline confirms. Treat as a distinct strategy if built.

## Reference
- `docs/TVB2_control_AB_rerun.md` (control 2x2 + ground-truth fee model; CANONICAL, supersedes the
  Downloads/TVB2_fee_handling.md draft)
- `docs/ATLAS_Timeframe_Continuity_Charter.md` (Section 0 first); `CLAUDE.md` (STRICT COMPLIANCE)
- `docs/HANDOFF.md` (TVB-2 entry at top); `docs/EXTERNAL_REVIEW_PROTOCOL.md`
- Memory: `tvb1-tv-mcp-reader-newui` (reader FIXED + new API ref), `tvb2-fee-resolved-real-taker`,
  `tvb1-controlB-fee-churn-finding`, `feedback-relaunch-is-cheap`
