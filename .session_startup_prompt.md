# Session Startup Prompt -- TVB-33

**Previous Session:** TVB-32 (2026-08-31 go-live + 2026-09-04 overnight
round-2 ledger analysis; the executor is STILL LIVE on the VPS)
**Current Branch:** `main`
**Commit state:** pushed (this repo docs; executor main = analysis commit,
executor branch fix/entry-invalidated-bar = staged fix, NOT merged)
**Plan Mode:** OFF for the morning review; ON if the runner/partial-harvest
or minimum-bar-range questions turn into a design session
**Prior-session review:** REQUESTED -- next session checks
docs/reviews/tvb32-codex-audit.md (and tvb31, still unreturned)

---

## READ FIRST

The round-2 live run has been trading since Mon 2026-08-31 15:37 UTC and
was deliberately left running Thursday night. The overnight analysis is in
hip3-executor `runs/2026-08-31_round2/ANALYSIS.md` (+ analysis.json) and
the morning-review Artifact page. Three findings: (1) the entry gate admits
bars that are already Type 3 (fix staged on branch
`fix/entry-invalidated-bar`, cbea184, 95/95 tests, NOT deployed); (2) the
flip exit again resolved stop-first 12/13, the midnight-UTC coupling is
real but the decoupled variant did worse; (3) books tie to the venue
within $0.003 and the equity formula is pinned (true equity = spot USDC
total; account_value() double-counts main-dex margin).

## NEXT (in order)

1. MORNING REVIEW with the user (their scrutiny first -- the analysis is
   UNREVIEWED by them). Walk the three findings and the four
   walkthroughs (ACE, JUP, SPX, PAXG) in trader terms.
2. Decisions (user-owned): (a) merge + deploy `fix/entry-invalidated-bar`
   -- deploy = merge to main, `deploy_from_dev.ps1`, restart the tmux
   loop (DEPLOYED_SHA full 40-hex; SSH = explicit go); (b) code the
   equity-display fix (`account_value()` = spot USDC total while
   all-isolated) + verify vs the HL app once; (c) the 1-3 Rev Strat
   trigger ruling (scanner far-side vs skill R22 reclaim); (d) weekend:
   keep running or KILL_FLAT before Fri 18:00 ET (weekend protocol).
3. If the run is closed: KILL_FLAT on the user's word, zero/zero receipt,
   refresh the snapshot (`analysis/fetch_round2.py` + `round2.py`) so the
   ledger is CLOSED, not a snapshot; commit under runs/2026-08-31_round2/.
4. Design-lane candidates (prereg before code, a-priori only): minimum
   prior-bar range for a tradeable level (mid-vs-trade noise on
   hair-thin bars); min stop distance / fee-aware R:R for max-notional
   clamped tickets; runner / partial-harvest profile (JUP give-back);
   funding-aware holding cost (ACE $0.21 on $10).
5. Carried: month-end fresh-window regen (overdue since ~Sep 1); TVB-31
   AND TVB-32 reviews when returned; TV mirror on demand; TVB-18 repairs;
   jackson set_inputs fix; tvb8/tvb9 unreturned.

## Open questions carried from the charter (Section 8)

- Anchor clock + weekend-protocol arms: pre-registered, not run.
- Equity-side drift reference: deliberately unchosen; xyz runs unfiltered.
- Exit-design lane: runner profiles past T1 -- now with a live JUP case.

## Reference

- C:\Strat_Trading_Bot\hip3-executor (README STATUS: items b/e/f closed by
  TVB-32 data; runs/2026-08-31_round2/ANALYSIS.md; branch
  fix/entry-invalidated-bar)
- docs/reviews/REVIEW_REQUEST.md (TVB-32), docs/reviews/tvb30-codex-audit.md
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first)
- CLAUDE.md (STRICT COMPLIANCE)
