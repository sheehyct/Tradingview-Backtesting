# Session Startup Prompt -- TVB-33

**Previous Session:** TVB-32 (2026-08-31 go-live; 2026-09-04 overnight
round-2 ledger analysis; Friday morning the entry-gate fix was MERGED AND
DEPLOYED and the executor restarted -- STILL LIVE on the VPS)
**Current Branch:** `main`
**Commit state:** pushed (this repo docs; executor main 4b5d248 = merge
e2a91ad + README STATUS; the fix branch is merged)
**Plan Mode:** ON -- the user wants a DESIGN DISCUSSION first (entry/exit
mechanics across timeframes, pattern breakdown, candidate selection in
trends, the two round-2 rulings they are second-guessing)
**Prior-session review:** REQUESTED -- next session checks
docs/reviews/tvb32-codex-audit.md (and tvb31, still unreturned)

---

## READ FIRST

The round-2 live run has traded since Mon 2026-08-31 15:37Z and continues
under `source_sha 4b5d248` (restarted 2026-09-04 15:08Z with the
`entry_bar_invalidated` gate). Overnight analysis: hip3-executor
`runs/2026-08-31_round2/ANALYSIS.md` + analysis.json + the Artifact page
(link in the TVB-32 HANDOFF/session-end message). Three findings: entry
gate admitted already-Type-3 bars (FIXED, deployed); flip exit resolved
stop-first 12/13 again, midnight-UTC coupling real but the decoupled
variant did worse; books tie within $0.003 and the equity formula is
pinned (true equity = spot USDC total; `account_value()` double-counts
main-dex margin -- reporting fix NOT coded). User: "pleasantly surprised
with the performance so far".

## NEXT (in order)

1. CHECK THE RUN FIRST: tmux session `executor` on the VPS, last
   heartbeat, open positions, any `entry_bar_invalidated` refusals in
   data/live/decisions.jsonl (the new gate's first sightings), any
   `entry_blocked` / `protection_lost` rows. Weekend: crypto trades
   24/7, xyz is RTH-gated; KILL_FLAT only on the user's word.
2. DESIGN DISCUSSION (plan mode; trader terms + concrete bar-by-bar
   walkthroughs; strat-methodology skill loaded; STOP-and-ASK on any
   methodology ambiguity). The user's seeds, in their framing:
   a. Daily-pattern entries: T1/T2 on the daily as the only exits, OR
      enter on the daily, take the 1h targets first, then step up
      2h -> 4h -> 8h -> 12h as the trade trends ("bump up the timeframe
      with the trend"). Overlapping targets across timeframes are
      expected. Skill anchors: pivot ladder (5.1, DP1/WP1 confluence),
      management profiles (5.4, R15), continuation magnitude (R14/R18).
   b. Pattern breakdown: which entries/exits by pattern; the per-signal
      census in analysis.json is characterization input only.
   c. Candidate selection in big trends: daily 2U-2U-2U-2U runners are
      invisible to a rev-only + escalate-cont universe.
   d. Second-guessing (given the current overnight/geopolitical regime):
      the BTC daily-open drift veto for crypto, and RTH-only for xyz.
      The round-2 censuses for both pools are in ANALYSIS.md (drift pool
      positive only via 8 trades; RTH-outside pool zero-mean).
   e. The 1-3 Rev Strat trigger ruling (scanner far side vs skill R22
      reclaim) -- one SOL trade in round 2.
   Every change = a dated ruling + prereg before code; ablation, not
   tournament.
3. Visualize the trades (user: still owed) -- chart-level walkthroughs of
   the round-2 trades, e.g. via the TV MCP on the traded symbols or
   candle plots from the cached 1m data.
4. Code the equity-display fix (`account_value()` = spot USDC total
   while all-isolated) when the user says so; verify vs the HL app once.
5. Carried: month-end fresh-window regen (overdue since ~Sep 1); TVB-31
   AND TVB-32 reviews when returned; TV mirror on demand; TVB-18 repairs;
   jackson set_inputs fix; tvb8/tvb9 unreturned. Later (user): basic
   regime identification for metals (silver/gold) and tech vs 2y/10y
   yields.

## Open questions carried from the charter (Section 8)

- Anchor clock + weekend-protocol arms: pre-registered, not run.
- Equity-side drift reference: deliberately unchosen; xyz runs unfiltered.
- Exit-design lane: runner profiles past T1 -- now with a live JUP case
  and the user's timeframe-walk idea.

## Reference

- C:\Strat_Trading_Bot\hip3-executor (README STATUS 2026-09-04;
  runs/2026-08-31_round2/ANALYSIS.md; analysis/round2.py)
- docs/reviews/REVIEW_REQUEST.md (TVB-32), docs/reviews/tvb30-codex-audit.md
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first)
- CLAUDE.md (STRICT COMPLIANCE)
