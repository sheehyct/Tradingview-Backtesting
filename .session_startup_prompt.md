# Session Startup Prompt -- TVB-14

**Previous Session:** TVB-13 (2026-07-17; TVB-12 fold-in + BF Comprehension
shipped + give-back v1)
**Current Branch:** `main`
**Commit state:** pushed to `origin` (PUBLIC) through the session-end commits
**Plan Mode:** exit-ablation pre-reg remains decide-WITH-user (draft ->
explicit approval -> run). The companion MFE/MAE Pine build is mechanical.
**Prior-session review:** RETURNED 2026-07-18, NEEDS-CHANGES, and the
critical synthesis is ALREADY WRITTEN (same-conversation addendum in the
HANDOFF TVB-13 entry). Do NOT redo the fold-in; execute the fix list.

---

## READ FIRST

POST-WRAP ADDENDUM (2026-07-18): the user restated the END GOAL (memory
project-live-arc-roadmap -- three tiers, then live HL micro-capital) and set
GO-FOR-IT mode for watch-layer builds (memory feedback-go-for-it-mode).
Tier-1 v1 was built and DEPLOYED same-day: "TFC Entry + BF Exit [TVB-13]"
(pine/tfc_bf_watch.pine; TV USER;7c28fa0b..., v2.0, live on the user's DRAM
5m watch pane) -- TFC entries (corrected clock, D/W/M gates, both dirs) +
BF-touch exits (completed-3 ADJACENT lines, 12h/D/W/M highest-first) + flip
backstop + alerts. The user tests it over the new week (new weekly
candidates). DAY-ONE OBSERVATION: when a rung's line runs THROUGH current
price (weekly line on DRAM), touch-exit + immediate re-entry CHURNS -- the
first candidate fix is a post-harvest re-entry block (ratchet concept).
Also still live: BF Comprehension (USER;84bb0f1e..., grade in ADJACENT mode
-- proximity anchors have the known HIGH staleness bug). Give-back leak
quantified (MFE 19.87% -> -1.18%, give-back 21.06pp), tests green.

## NEXT (in order -- USER-LOCKED 2026-07-18, plain English)

> GUARD (user's explicit instruction): the session goal is the ROADMAP, not
> the audit. Codex reviews are good for us ~95% of the time; the 5% trap is
> letting the fix list eat the session. Audit items ride along only where
> they overlap the build; pure label/cleanup items go last.

1. READ THE WEEKEND LOG FIRST. The user paper-traded the new weekly
   candidates on TFC-BF v3 (written entries/exits). That log is the
   session's primary input; everything below serves what it shows.
2. MAKE THE EXITS RIGHT (tier-1 refinement, one deploy cycle):
   a. DIRECTION-RELATIVE: shorts harvest only at lines BELOW the position;
      an overhead TTO'ed line touch is retest-rejection context (user
      screenshot evidence: rejections dead on the tick), never an exit.
   b. TOUCH CONFIRMATION (user's rough rule, dialable input, NOT concrete
      yet): in a tight range the line gets grazed repeatedly -- something
      like a full HOURLY candle close back beyond the line (for shorts:
      close above the lower line) confirms the event. Build as an input
      (confirm TF + close-vs-wick), dial later. This is the user's
      provisional answer to the reserved break vs break-and-return
      decision.
   c. TTO LADDER: when a line breaks ("TTO"), walk BACK to the next
      formation (BF2) as the operative harvest line -- pivot-ladder style;
      keep the broken line faded as a reclaim/retest level.
   d. Ride-along audit fixes IN THE SAME CYCLE where they touch the same
      code: bf_comprehension proximity re-scan through HTF close (+ the
      June-3 12h fixture: fire anchor 68.624 vs correct 67.15), freeze via
      time_close + gate L1, L4 same-bar restart + pin x2 at supersede.
3. START THE TIER-2 BRIDGE: get scanner strat targets onto the chart --
   manual input levels first is fine (the monthly 2U-2D target 41.00
   meeting the BF2 line IS the harvest-zone concept working; user
   screenshot). Automation (MCP input-push or Python-side join) after the
   manual proof.
4. KEEP ARCHIVING HL bars (floors slide daily; June was days from gone).
5. LAST, if budget remains: audit label/cleanup items (give-back gross +
   bar-envelope docstrings, summarize median, tiling qualifier, design-doc
   reconciliation), comparator v2, TVB-11 doc edits. NOTE: the C1
   exoneration SURVIVED hostile review; live-gate wording done; no new
   remediation beyond what is listed here.
2b. Collect the user's live grading of BF-COMP (their written paper-trade
   log + line-vs-hand-drawn verdicts per rung/mode). Divergences become
   numbered design questions. THEN draft the exit-ablation pre-reg for
   approval: controls (flip / state / swing-line BF) vs compound-3 BF
   ladder, pivot-ladder trim/caution, ATR/% trails. Open user decisions:
   break vs break-AND-return; single rung vs trim-across-rungs; no-touch
   fallback; line-update rule when a new 3 prints. (TF ladder largely
   settled: 60m floor, {60m,4h,12h,D,W,M}, HTF precedence.)
3. Companion MFE/MAE surface: port giveback semantics into
   pine/tfc_companion.pine (deferred from TVB-13 for deploy-cycle cost).
   Deploy trap notes below are load-bearing.
4. ARCHIVING (now demonstrably urgent): HL 15m floor was at May 26 when the
   DRAM June fixture was captured -- days from unfetchable. Stand up the
   recurring HL pull for the roster (MU + champions first); the give-back
   distribution over committed trade lists is BLOCKED on per-symbol bars.
5. Standing remediation: comparator v2 (TVB-12 C2) before any new DRIFT
   claims; collector digest + no-name-fallback + session assert (C3) before
   the next collection run; TVB-11 doc edits (F4/F5/F6/F7) still pending.

## Open questions carried

- Exit-ablation user decisions listed in priority 2.
- HANDOFF archiving DONE 2026-07-17 (user-approved): TVB-0..9 live verbatim
  in docs/session_archive/HANDOFF_TVB0-TVB9.md; HANDOFF keeps TVB-10+.
- PARKED (do not resume without user): combined winner indicator, regime
  label, HTF viewability, date-range expansion.

## Operational notes (hard-won, this session)

- NEW TRAP: jackson indicator_set_inputs CORRUPTS indicator() studies
  ("Can't parse pine"; getInputValues returns null on indicators --
  strategies are fine). Use the study SETTINGS DIALOG via UI (double-click
  legend row); recovery = remove + re-add the study. Memory:
  tv-mcp-pine-tab-binding-trap (new facet).
- Save trap CONFIRMED again: saves persist only in the FIRST Pine-dialog
  session per TV launch; verify by version bump; full recipe in the same
  memory. TV Store-app relaunch: Stop-Process TradingView, then Start-Process
  on (Get-AppxPackage TradingView.Desktop).InstallLocation with
  --remote-debugging-port=9222.
- BF-COMP fixture state: freeze ON at 2026-06-04T00:00Z, L1 chars OFF,
  12h+D rungs, line mode ADJACENT (last toggle). The user can flip
  everything in settings. The DRAM 15m pane formerly held NQ.
- scripts/tv_probe.mjs history = the fix when chart_set_visible_range
  snaps back (lazy history not loaded).
- Winner watch surfaces are FROZEN (header notes; three are entry-dead at
  chart==ARM_TF, ShortChamp misses final-child breaks; backtest record
  unaffected -- resting stop orders). Corrected clock pattern (roll AFTER
  test + isconfirmed exits) is MANDATORY in any TVB-14 arming port.

## Reference

- docs/HANDOFF.md TVB-13 entry (top: synthesis, remediation queue, BF arc)
- docs/design/bf_comprehension_indicator_design.md (the shipped design)
- analysis/giveback.py + tests/test_giveback_fixture.py (the leak metric)
- docs/ATLAS_Timeframe_Continuity_Charter.md Section 0 FIRST; CLAUDE.md
  (STRICT)
- Memory: project-exit-arc-tvb13, project-compound3-bf-redesign (Rob anchor
  rule), tv-mcp-pine-tab-binding-trap, feedback-results-plain-language
