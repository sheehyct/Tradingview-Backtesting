# Session Startup Prompt -- TVB-25

**Previous Session:** TVB-24 (2026-08-15, autonomous overnight)
**Current Branch:** `main`
**Commit state:** pushed (session range `674c7f6^..` -- concrete head pinned in docs/reviews/REVIEW_REQUEST.md)
**Plan Mode:** ON if the user opens the exit-design lane; OFF otherwise
**Prior-session review:** REQUESTED -- next session checks docs/reviews/tvb24-codex-audit.md

---

## READ FIRST

TVB-24 closed BOTH pending reviews and the one open plan step. The TVB-23
Codex audit was folded with every finding reproduced before adjudication
(guards hardened fail-closed + 17 adversarial tests; the two prereg-bound
diagnostics delivered as committed receipts; dated corrections). The
2026-08-14 strategy-implementation assessment was folded the same way
(both diagnostics reproduced to the decimal; entry_audit receipts;
CLAUDE.md/README no longer deny the Python engine). The TV mirror + 9-cell
re-gate PASSED (218/218 events, all six arms now TV-valid) -- prereg step
7 is CLOSED. The research fork is now genuinely open and needs the USER'S
direction; nothing blocks it.

## NEXT (in order)

1. Fold the TVB-24 review if returned (docs/reviews/tvb24-codex-audit.md).
2. Research fork per user direction -- three candidate lanes, none
   pre-selected: (a) exit-design (plan mode ON; bimodal ladder 39% stall /
   37% run unsolved; retracement census + matched-exit receipt are the
   evidence base; canonical intrabar-3 invalidation exit still ABSENT);
   (b) fresh-window replication (own prereg; the biggest evidence gap --
   everything is one gross in-sample window); (c) the isolated
   C0-current/C1-current pair (assessment P0-1: the C1-vs-C0 charter rung
   was never run; identical entries/costs, exit policy the only delta).
3. Assessment adoption decisions the user still owns: the kernel-vs-Pine
   charter question (parked; owner-level), 1m/trade archiving start for
   future causal-fill work, spine CLAUDE.md project-map row still says
   "Pine-first; Python = analysis layer" (stale, outside this repo).
4. Greenlit repairs bundle (TVB-18, carried): F2 roster receipts +
   fail-closed, F3 5m-lifecycle warm-up regression, F4 eviction telemetry
   split, freeze-boundary invariant, SKHX tv_symbol/mintick backfill.
5. Nudge the M+T collaborator (PMG+ prefix unreachable); jackson MCP
   indicator_set_inputs full-array fix (jackson-repo session).

## Open questions carried from the charter (Section 8)

- Anchor clock (UTC vs RTH) arms still pre-registered, not run.
- Weekend/low-liquidity protocol arms still pre-registered, not run.
- S3.1 stands (A1F +23.0 still -81.8pp under A0b; never adjudicated by
  package arms).
- Exit-design revisit gated on the user's visualization pass; NEW
  evidence since TVB-23: the matched-exit receipt shows realized P&L
  rising strictly with depth on the 37 trades closed in all six arms
  (38.4 -> 66.2pp) -- the whole-arm shallow-top is an occupancy effect,
  not a per-trade exit effect (survivor-shaped subset, boundary declared).

## Workbench state (2026-08-15 close)

TV Desktop RUNNING with CDP 9222; NEW layout "TVB24-mirror" active
(created this session -- the user's layouts untouched; their SNDK live
tab never driven); package strategy mounted on the workbench chart (last
symbol DRAM 5m, arm D1ATR); editor bound to the package script, TV source
byte-equal to committed HEAD (sha-verified). Screenshots hang while the
Windows session is locked -- drive blind via DOM probes (see memory
tv-mcp-tvb24-ops).

## Reference

- docs/experiments/tvb23_t1floor_report.md (incl. 2026-08-15 corrections)
- analysis/paper/tier_b_t1floor/ (all receipts incl. atr_context,
  matched_exit, entry_audit)
- docs/strategy-implementation-assessment.md + docs/reviews/tvb23-codex-audit.md
  (both folded; synthesis in HANDOFF)
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first)
- CLAUDE.md (STRICT COMPLIANCE)
