# Session Startup Prompt -- TVB-32

**Previous Session:** TVB-31 (2026-08-30 -- TVB-30 BLOCK audit folded
same-day across all three repos; scanner deploy VERIFIED not redeployed;
HyPaper spike committed)
**Current Branch:** `main`
**Commit state:** pushed (concrete shas pinned in docs/reviews/REVIEW_REQUEST.md)
**Plan Mode:** OFF for the live-ops checklist; ON only if the HyPaper
discussion turns into a design/adoption session
**Prior-session review:** REQUESTED -- next session checks
docs/reviews/tvb31-codex-audit.md

---

## READ FIRST

TVB-31 folded the TVB-30 audit (verdict BLOCK, 0C/4H/4M/4L -- all
findings reproduced first, 10/10 executor probes -> 0/10, zero
disputes; two new dated rulings 2026-08-30: leverage-unverified =
warn-only, actual-fill notional = receipt + warn past 5% over cap).
Everything is MERGED: executor 4e384bb (90 tests), this repo 371c37d+
(287), scanner PR #10 @ 7723462 (356; test-only, no deploy needed).
The scanner Railway item was resolved by VERIFICATION: cont targets are
already live (payload probe + display_v hash match + SMOKE GREEN);
`railway up` deliberately skipped as a no-op restart. Monday =
short HyPaper discussion FIRST, then the go-live checklist.

## NEXT (in order)

1. DISCUSSION (user-flagged): HyPaper adoption. Spike DONE
   (docs/experiments/tvb31_hypaper_spike.md): crypto-only (zero
   builder-dex support), NOT drop-in (wallet-field 400 -> ~30-line
   shim; trigger orders DEAD via its API -> ~2-line upstream patch;
   HTTP-400-on-err -> ambiguous class; broker.py needs base_url cfg;
   perp_dexs=[""] pin or phantom xyz positions); fills = L2-VWAP priced
   (better than PaperBroker) with named optimisms (mid-not-mark
   triggers, no maker queue, no partials, no liquidations). Decide:
   adopt (then the integration pass + one probe bracket) or park.
2. Executor deploy to the ATLAS VPS via deploy/deploy_from_dev.ps1
   (4e384bb or later; DEPLOYED_SHA must be the full 40-hex sha -- the
   startup provenance now REJECTS anything else as deployed_sha_invalid).
   SSH touches production -- explicit user go per spine rules.
3. VPS dry-run: refusal reasons (reach_unavailable, counter_drift
   crypto-only, target_beyond_reach, underlying_closed) + cont targets
   resting in paper brackets; KILL_FLAT drill flat (clean receipt +
   failed_closes + NEW order_sweep field + halt). Then
   `rm data/KILL_FLAT` DELIBERATELY, set live env, start.
4. Supervised probes = first live trades: one bracket with verified
   resting stop -- ALSO confirm the frontendOpenOrders stop-row field
   VALUES (orderType/isTrigger/reduceOnly) against the real resting
   bracket (README STATUS item f; the stop-contract predicate asserts
   them from SDK docs); risk_usd_booked/stop_venue/notional_usd_filled
   in the entry row; equity formula vs venue UI with exactly ONE
   isolated position (STILL UNVERIFIED); first xyz fill (dex-scoped
   coin naming AND whether user_fills carries builder-dex fills).
5. Run to day end; KILL_FLAT on the user's word; zero/zero receipt;
   ledger -> runs/2026-08-31_monday/.
6. Carried: month-end fresh-window regen ~Sep 1 (re-pins TVB-28 deltas
   + TVB-30 rollup fields + NEW TVB-31 per-symbol net fields, 0.0001pp
   class); TVB-31 review fold when returned; TV mirror on demand;
   TVB-18 repairs; jackson set_inputs fix; tvb8/tvb9 unreturned.

## Open questions carried from the charter (Section 8)

- Anchor clock + weekend-protocol arms: pre-registered, not run.
  Friday late-day OPEX pinning stands as a second a-priori dead zone.
- Equity-side drift reference: deliberately unchosen (future a-priori
  choice); xyz runs unfiltered.
- Exit-design lane: runner profiles past T1 remain a future prereg.

## Reference

- C:\Strat_Trading_Bot\hip3-executor (README = Ruleset v1 + 2026-08-28
  + 2026-08-30 amendments + honest gate STATUS incl open items e/f;
  tests/ = 90)
- docs/reviews/tvb30-codex-audit.md (folded) + REVIEW_REQUEST.md (TVB-31)
- docs/experiments/tvb31_hypaper_spike.md (the Monday discussion input)
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first)
- CLAUDE.md (STRICT COMPLIANCE)
