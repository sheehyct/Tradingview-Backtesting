# Session Startup Prompt -- TVB-6

**Previous Session:** TVB-5 (2026-07-03)
**Current Branch:** `main`
**Commit state:** TVB-5 committed + PUSHED to `origin` (PUBLIC) through the session-end
commit. Sweep complete; the two-layer characterization is now four-sample deep.
**Plan Mode:** OFF for the xyz verification mechanics (it is a data-comparison task);
ON only if the verification forces a data-source design fork (e.g. adopting xyz as the
primary backtest chart).
**Prior-session review:** REQUESTED -- check `docs/reviews/tvb5-codex-audit.md` at start
(via `docs/reviews/REVIEW_REQUEST.md`, the reviewer pointer).

---

## READ FIRST
TVB-5 ratified S8 (stand_aside; reg_mode stays an input, default off), folded in the TVB-4
Codex review, and ran the pre-registered 3x3 timeframe-set sweep across FOUR samples
(35 runs). The system is now characterized as a REGIME-LOCAL edge (MSTR-class, trending
periods only; dead on BTC everywhere; negative gross in MSTR Dec-Feb chop) with a
UNIVERSAL damage-containment layer (M/W/D stand_aside improved every pairing in every
sample). THE open question: xyz MSTRUSDC.P (target venue, TV backfill to 2025-12-02)
shows the SAME strategy at **+80.2% where the OKX proxy shows -9.1%** (shared window,
ctrlB @0.0125, near-identical trade counts). If real, OKX was the conservative proxy all
along; if artifactual, every xyz number dies. Verify before ANYTHING else builds on it.

## NEXT (in order)
1. **Verify the xyz TV backfill against Hyperliquid SDK candles (Python analysis layer).**
   Pull HL candle history for the xyz dex MSTR perp (HIP-3 dex candles via the HL API/SDK),
   export the TV bars (data_get_ohlcv or a CDP probe; `tvb4_bars_60m.json` precedent), and
   compare OHLC bar-by-bar over the shared window. Deliverable: is the venue gap real
   price behavior (wick/basis structure on the thinner oracle-priced book) or a data
   artifact (synthesized/sparse backfill)? Charter S0: it is a question, not a verdict.
2. **If (and only if) the gap is real:** decide with the user whether xyz MSTRUSDC.P
   becomes the primary backtest chart (venue-native data dissolves the S7.3 venue-mismatch
   trap for MSTR); re-read the headline rows there. If artifactual: document the failure
   mode and keep OKX as canonical.
3. **Deferred carries (unblocked once 1 resolves):** short-leg MAE/solvency vs 1x cash
   margin BEFORE any deployability language (Codex TVB-3 finding 2); slippage realism at
   ~90-contract size; re-entry governor (charter S8, still open).

## Open questions carried from the charter (Section 8)
- Grey handling: DECIDED + RATIFIED (stand_aside; input default stays off -- regression
  anchor). S8 ordering held in all four TVB-5 samples including the 2.5y BTC spot-check.
- Regime-speed: NEW finding -- monotone-destructive with speed (M/W/D > W/D/12h > D/12h/4h
  everywhere). A regime SLOWER than M/W/D was NOT in the grid -- noted as a gap; do not
  tune it in mid-sample, it would be a new a-priori candidate for a future sweep.
- Re-entry governor -- still open. Price-stop vs state-stop by horizon -- still open
  (bar-magnifier re-check required if a price-stop variant is built).
- Vol/trend-conditional grey rule -- still PARKED (charter S6 bar unmet).

## Operational notes (hard-won)
- **TradingView is now a Microsoft Store app** -- `tv_launch` auto-detect FAILS. Launch:
  `Start-Process (Join-Path (Get-AppxPackage TradingView.Desktop).InstallLocation
  'TradingView.exe') -ArgumentList '--remote-debugging-port=9222'` (kill any running
  instance first; a bare relaunch does not attach the port).
- Re-stage flow: set OKX:MSTRUSDT.P 15m, ui_open_panel pine-editor, pine_open
  "TFC Baseline", click Add to chart (ui_find_element text + ui_mouse_click -- the
  data-name selector does not match), RE-DERIVE entity id (was `bNqlkZ`, changes every
  add). Id map UNCHANGED at pineVersion 18: exec in_2..in_13 (M/W/D/60/30/15 pairs),
  in_16 reg_mode, in_18/20/22 regime TFs, in_23 grey=50, in_24 reg_exit, in_32 commission,
  in_33 slippage=1, in_34/35 margins 0/0, in_41 magnifier off.
- xyz feed on TV: exchange "Trade[XYZ] HIP-3 Hyperliquid"; bare ticker resolves
  ("MSTRUSDC.P"); the display-name prefix does NOT. MSTRUSDC.P backfilled to 2025-12-02;
  SP500USDC.P has NO backfill (recording started 2026-07-03 18:00Z -- becomes usable as
  weeks accrue). OKX BTC 15m floors ~2025-12-01 (~20.6k bar cap); BTC 60m reaches
  2024-01-01.
- `node scripts/tv_probe.mjs history` before EVERY read; `node scripts/tv_dump.mjs <out>`
  now reports CLOSED-basis L/S (open trade = pseudo-closed list row, mark-to-market exit
  at wall-clock ms timestamp; closed set = first totalTrades entries).
- `analysis/window_compound.py`: window-sliced product(1+pp) over dump trade lists --
  shared-window/prefix comparisons WITHOUT re-running. Unit-tested (suite 9/9).
- Chart left on OKX:MSTRUSDT.P 15m, ctrlB config, reg_mode=off, fee 0.0125 (resting
  state); end-state regression green vs tvb5_R0b.

## Reference
- `docs/TVB2_control_AB_rerun.md` (canonical datasheet; TVB-5 pre-registration + results
  sections at the end -- the sweep tables, scorecard, and surprises live there)
- `docs/ATLAS_Timeframe_Continuity_Charter.md` (Section 0 first); `CLAUDE.md` (STRICT
  COMPLIANCE); `docs/HANDOFF.md` (TVB-5 entry at top)
- Memory: `tvb4-two-layer-regime-ablation`, `tvb3-stale-gate-margin-artifacts`, this
  session's TVB-5 entry
