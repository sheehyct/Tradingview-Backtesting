# Session Startup Prompt -- TVB-8

**Previous Session:** TVB-7 (2026-07-04)
**Current Branch:** `main`
**Commit state:** TVB-7 committed + PUSHED to `origin` (PUBLIC) through the
session-end commit. No sibling-repo changes this session (jackson `27757bc`
still local-transport-only, loads on MCP restart).
**Plan Mode:** OFF -- the VBT port design is APPROVED (docs/VBT_BREADTH_PORT_PLAN.md);
implementation follows the plan. Re-enter plan mode only if implementation
uncovers a design-level deviation (surface it first, charter S0).
**Prior-session review:** REQUESTED -- check `docs/reviews/tvb7-codex-audit.md`
at start (via `docs/reviews/REVIEW_REQUEST.md`, the reviewer pointer).

---

## READ FIRST
TVB-7 closed every startup priority AND the Codex TVB-6 fold-in. Governor v2 is
now CROSS-VENUE-VERIFIED on the MSTR underlying (direction non-negative 8/8
paired OKX/BTC cells; magnitude path-local, +12pp xyz -> +2-3pp OKX). The
Codex fold-in produced a load-bearing correction: **`strategy.closedtrades.
profit()` is GROSS of commission** (575-trade identical prefix at 1% fee; all
governed results stand as-deployed, but the "net of fees" description was
wrong and the VBT port spec was corrected before it could fail its own gate).
Cost realism: live L2 at ~90 contracts prices per-fill cost BETWEEN s25 and
s50 (weekend-night book, conservative); funding INVERTS the fee gradient
(slow cells pay ~2x churn cells; -2.5 to -4.9pp over the sample). The single
most important next move: **execute the VBT breadth port to its Phase-3 gate
in a vectorbt-workspace session.**

## NEXT (in order)
1. **VBT port implementation, Phases 0-3** (the trade-for-trade gate) per
   `docs/VBT_BREADTH_PORT_PLAN.md` -- runs in a VECTORBT-WORKSPACE session
   (its CLAUDE.md/hooks govern; backtesting-validation + strat-methodology
   skills; VBT 5-step only at the optional VBT touchpoint). The plan carries
   calibrated facts (qty rule, pv formula, GROSS arming) -- treat as spec.
   POLICY: no Python breadth number enters the record before the 8 gate cells
   are green. This repo's role: frozen reference artifacts; capture an R2
   size_down dump ONLY if the port decides to gate size_down (optional).
2. **Weekday-RTH L2 re-sample** (`analysis/l2_book_impact.py`, same ladder)
   -- the weekend sample is the conservative bound; the weekday sample is the
   operative point estimate. Compare; update the datasheet section.
3. **Pine hygiene at next deployment** (bundled, not standalone): correct the
   governor comment (lines ~212-219) to GROSS-arming semantics; re-run the
   byte-identity prefix regression. OPTIONAL follow-on if user wants it: pre-
   register a net-arming variant (profit - closedtrades.commission) as a
   charter-S5 ablation vs the as-deployed gross-arming governor.

## Open questions carried from the charter (Section 8)
- Re-entry governor: KEPT, cross-venue-verified (MSTR underlying); arming is
  GROSS as deployed; net-arming variant = open ablation candidate (pre-
  registration required). gov_mode default stays `off` (regression anchor).
- Grey handling: RATIFIED (stand_aside). Regime speed: monotone-destructive.
- Price-stop vs state-stop by horizon -- still open (bar-magnifier re-check
  required if a price-stop variant is built).
- Vol/trend-conditional grey rule -- still PARKED (charter S6 bar unmet).

## Operational notes (hard-won, verified this session)
- TV launch (Store app): kill any running TradingView, then
  `Start-Process (Join-Path (Get-AppxPackage TradingView.Desktop).InstallLocation
  'TradingView.exe') -ArgumentList '--remote-debugging-port=9222'`.
- Slot "TFC Baseline" pineVersion 20.0, entity `SCn1V9` (survives symbol/TF
  changes; dies on TV restart -- re-derive via scratchpad tv_inputs_map).
  ID map: in_9/11/13 exec TFs, in_16 reg_mode, in_25 gov_mode, in_33 comm,
  in_34 slip, in_35/36 margins 0/0, in_42 magnifier(false).
- `node scripts/tv_probe.mjs history` before EVERY read; dumps via
  `node scripts/tv_dump.mjs <out>`; tv_bars.mjs now persists tick metadata.
- Chart resting state: HIP3XYZ:MSTRUSDC.P 15m, ctrlB defaults, reg/gov off,
  fee 0.0125, s1 -- anchor ~+8.0-8.2%/4,310-4,312 closed (live tail drifts).
- OKX MSTR data floor is 2026-02-25 on BOTH 15m and 60m (listing-driven).
- xyz convention: churn-config numbers at s1 AND s10, never s1 alone.

## Reference
- `docs/VBT_BREADTH_PORT_PLAN.md` (approved design of record; corrected spec)
- `docs/TVB2_control_AB_rerun.md` (TVB-7 sections: cross-venue verification,
  cost realism, Codex synthesis + gross-arming adjudication)
- `docs/ATLAS_Timeframe_Continuity_Charter.md` (Section 0 first); `CLAUDE.md`
  (STRICT COMPLIANCE); `docs/HANDOFF.md` (TVB-7 entry at top)
- Memory: `tvb7-crossvenue-costs-portplan`, `project-vbt-breadth-engine`,
  `user-leverage-philosophy`, `project-one-layer-screener-attach`
