# Session Startup Prompt -- TVB-24

**Previous Session:** TVB-23 (2026-08-10/12)
**Current Branch:** `main`
**Commit state:** pushed (session range `0023852^..` -- concrete head pinned in docs/reviews/REVIEW_REQUEST.md)
**Plan Mode:** OFF for the TV mirror (build against the committed prereg's
step-7 spec); ON only if the user opens the exit-design lane
**Prior-session review:** REQUESTED -- next session checks docs/reviews/tvb23-codex-audit.md

---

## READ FIRST

TVB-23 folded the TVB-22 audit (parity gate hardened fail-closed, ladder
census now receipt-backed), then ran the full T1-floor round under a
prereg committed BEFORE code: the floor repairs the package (D1 +83.8pp
vs A2 +24.6) but the matched control still leads (A0b +104.8 -- C2F does
not earn its place over C1); risk shape transforms (maxDD 22 vs 122);
ATR vetoes dissolve the chop symbol filter; the retracement census
supports the user's prior that a retracement exit fires too early. THE
ONE UNFINISHED PLAN STEP is the TV mirror + re-gate: until the package
strategy pine gains the floor/ATR/arm-selector options and passes its
9-cell parity gate (GOOGL/TSLA/DRAM x {D1, DINF, D1ATR}), the mounted TV
strategy is valid ONLY for the TVB-22 arms (declared in the prereg).

## NEXT (in order)

1. TV mirror + re-gate (prereg step 7): floor input + Wilder ATR(14) on
   the local 1H aggregation + ATR-veto inputs + new arm-selector entries
   (COMMA-FREE labels; setInputValues partial-array only) into
   pine/tfc_mt_package_strategy.pine, semantics verbatim from engine.py;
   Make-a-copy is NOT needed (same script evolves) but verify editor
   binding before save; then scripts/tvb22_pkg_harvest.mjs + the hardened
   analysis/paper/pkg_parity.py over the 9 new cells. TV strategy stays
   on TVB-22 arms until PASS.
2. Fold the TVB-23 review if returned (docs/reviews/tvb23-codex-audit.md).
3. Next research round per user direction: exit-design lane (plan mode ON;
   bimodal ladder 39% stall vs 37% run has no designed answer; retracement
   census + tvb23 receipts are the evidence base) OR fresh-window
   replication (own prereg; the out-of-sample gap is the biggest one).
4. Greenlit repairs bundle (TVB-18, carried): F2 roster receipts +
   fail-closed, F3 5m-lifecycle warm-up regression, F4 eviction telemetry
   split, freeze-boundary invariant, SKHX tv_symbol/mintick backfill.
5. Nudge the M+T collaborator (PMG+ prefix unreachable); jackson MCP
   indicator_set_inputs full-array fix (jackson-repo session).

## Open questions carried from the charter (Section 8)

- Anchor clock (UTC vs RTH) arms still pre-registered, not run.
- Weekend/low-liquidity protocol arms still pre-registered, not run.
- S3.1 stands and is reinforced (A1F +23.0 still -81.8pp under A0b).
- Exit-design revisit stays gated on the user's visualization pass; the
  retracement census (now run: labels fire before 0-1.3 rungs of
  progress, 100% of losers AND 48-96% of winners labeled) is its
  evidence base. Canonical intrabar-3 invalidation exit is ABSENT from
  the implemented exit set (flagged 2026-08-12 discussion) -- a future
  pre-registered variant if the user wants it.

## Reference

- docs/experiments/tvb23_t1floor_report.md (the round's findings)
- docs/experiments/tvb23_t1floor_prereg.md (binding spec incl. step 7)
- analysis/paper/tier_b_t1floor/ (rows, rollups, events, census receipts)
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first)
- CLAUDE.md (STRICT COMPLIANCE)
