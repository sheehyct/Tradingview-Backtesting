# Session Startup Prompt -- TVB-34

**Previous Session:** TVB-33 (2026-09-04/05): round 2 CLOSED by KILL_FLAT
on the user's word (interlock `data/KILL_FLAT` in place; funds withdrawn);
reject dig; twelve rulings prereg'd; the ledger replay harness BUILT
(executor main; 1,144 tests); round-2 parity PASS after three fidelity
amendments; nine arms receipted; the user ruled round 3 = v1 + the
fee-aware floor with everything else shadowed, and it is BUILT and
smoke-tested on executor main (NOT deployed).
**Current Branch:** `main` (this repo); executor `main` = the deployable
round-3 state (feat/round3-fee-floor merged)
**Commit state:** pushed (both repos)
**Plan Mode:** OFF for the deploy (checklist below); ON for the two open
rulings if the user raises them
**Prior-session review:** REQUESTED (docs/reviews/REVIEW_REQUEST.md,
TVB-33); TVB-31 and TVB-32 still unreturned

---

## READ FIRST

The executor is DOWN by design (KILL_FLAT interlock; never restart it or
rm the interlock without the user). Everything the user needs to rule on
is in docs/ARM_LEDGER.md (Live executor family) and hip3-executor
runs/2026-09-04_replay1/REPLAY.md (dual-language). The replay is a
characterization instrument; nothing in it is a promotion.

## NEXT (in order)

0. DONE 2026-09-06: the deep-dive review was returned and FOLDED (HANDOFF
   TVB-33 section 7). Executor main now carries the liquidation-aware
   clearance, the broker defenses, the dead-sponsor license repair, and
   the halfway synthesizer fix (re-run receipts in runs/2026-09-04_replay1,
   pre-fix copies in before_amend_j/). Read section 7 before step 1; the
   user rulings it raises (3-1-2 cont target, 3-2-2 stop, nested-inside
   mother, continuity stack, the no-progress exit as A10) are the user's
   call and do NOT block go-live unless the user says so.
1. ROUND 3 GO-LIVE (the PACKAGE: fee floor + weekly dot + four seats + $1 risk on $200 + repairs; executor README STATUS 2026-09-06), each step on the user's word (executor README STATUS
   2026-09-05 has the checklist): the user funds the dedicated wallet ->
   deploy executor main with deploy_from_dev.ps1 (full 40-hex
   DEPLOYED_SHA; SSH only with explicit confirmation; PS 5.1 stderr trap
   known) -> on the VPS `uv run python -m hip3_executor --once` with
   data/KILL_FLAT present = the interlock receipt (startup row with
   `arms` {ruleset v1+A9}) -> equity check once (hourly `equity` tracker
   row spot_usdc_total vs the HL app) -> `rm data/KILL_FLAT` on the
   user's word -> tmux `executor` `--live` -> confirm the first heartbeat
   and the first decision row carrying rr_net / fee_rt_pct. No supervised
   probes are needed (no venue primitive changed). Crypto trades from the
   start instant; xyz waits for the Monday bell.
2. Once live: watch the first refusals -- `reward_risk_below_floor` rows
   whose `rr` >= 1.0 but `rr_net` < 1.0 are the arm binding (expected on
   the gold-class xyz tickets); `fee_rate_unavailable` must NEVER appear.
3. Add the round-3 LedgerSpec to the replay (analysis/replay/ledger.py,
   contrast_control "as_built"), reading the journaled shadows first, so
   the round-3 ledger can be replayed at close against round 2.
4. The two open rulings when the user wants them: weekend-1 P5 (slippage
   on two thin trades) and the A5 halfway convention (empty tier under
   D4). Then scanner PR-B / PR-A, the profile arms, a daily-entries-only
   walk-up variant.
5. Carried: month-end fresh-window regen (overdue); TVB-31/32/33 reviews
   when returned; TV mirror on demand; TVB-18 repairs; jackson set_inputs
   fix; metals / tech-vs-yields regime ID (user, later); trade
   visualization still owed; replay `_Context` offline-pure.

## Standing constraints

- Never hardcode the VPS IP (use `<VPS_IP>`); VPS SSH only with explicit
  user confirmation; the master wallet address lives only in the private
  executor repo; `scripts/secret_scan.py` before every push here.
- Every rule change = a dated ruling + prereg BEFORE code; single-change
  arms; the ledger replay receipts an arm before capital.

## Reference

- C:\Strat_Trading_Bot\hip3-executor (branch feat/replay-harness:
  analysis/replay/CONTRACT.md, runs/2026-09-04_replay1/PREREG.md +
  REPLAY.md; README Ruleset v2 block)
- docs/experiments/tvb33_round3_prereg.md (rulings, arms, amendments
  b-h, the two OPEN questions)
- C:\Users\Chris\.claude\plans\glimmering-puzzling-quilt.md (approved plan)
- docs/reviews/REVIEW_REQUEST.md (TVB-33)
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first)
- CLAUDE.md (STRICT COMPLIANCE)
