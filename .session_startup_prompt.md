# Session Startup Prompt -- TVB-2

**Previous Session:** TVB-1 (2026-06-28)
**Current Branch:** `main`
**Commit state:** TVB-1 + external-review setup committed; remote wired + PUSHED to `origin` (PUBLIC).
**Plan Mode:** ON for the two-layer design (continuity logic = strat-methodology STOP-and-ASK zone).

---

## READ FIRST
TVB-1 built and characterized the baseline control. KEY FINDING: the 60/30/15 gate (15m) is a
-81% churn machine (PF 0.645 with fees, but PF 1.284 zero-fee -> the raw edge is real, fees kill
it); the slower M/W/D/60 gate (60m) trades ~9x less and clears OKX fees (PF 1.131) but NOT HIP-3
fees (PF 0.921). **The naive baseline is not deployable at real venue costs.** The single most
important next move: raise PER-TRADE edge via the two-layer architecture (a slow regime gate
permitting a fast execution layer) so entries are more selective, then re-test at HIP-3 fees (0.10%).

## NEXT (in order)
1. **Finish the TV MCP reader.** A full Claude Code relaunch loads the staged `data.js`
   diagnostic; read the new TV Strategy Tester data API from its dump, apply the data-access fix,
   relaunch -> clean JSON reads (end the screenshot wrangling).
   (`tradingview-mcp-jackson/src/core/data.js`; finder fix already done.)
2. **Codex-audit actions (do FIRST -- they may change what gate we tested).** See the "Codex
   external audit + Claude critical review" block in `docs/HANDOFF.md`, with Codex's full verbatim
   text in `docs/reviews/tvb1-codex-audit.md` (don't assume either is right). (a) ***VERIFY HTF-open currency** -- overlay the strategy's
   FTFC bar-coloring vs the TFO indicator (lookahead_on); if they differ, our `lookahead_off` opens
   were stale. (b) Adopt the unambiguous local period-open reconstruction
   `ta.valuewhen(timeframe.change(tf), open, 0)` regardless, plus the trigger tick-offset
   (`high + syminfo.mintick`) and a chart-TF<=enabled-TF guard. (c) RERUN A/B fee characterization
   to confirm TVB-1 findings hold.
3. **Design + build the TWO-LAYER strategy WITH the user** (charter Section 3.3): regime layer
   (e.g. M/W/D) permits/sizes; execution layer (60/30/15) signals. Re-test vs the controls at
   HIP-3 fees (0.10%). This is the prime candidate to raise per-trade edge. STOP-and-ASK zone.
4. If time (as ablations vs the control, NOT baked in): the "TFC-at-the-break" variant (lower chart
   TF + reconstructed execution-TF trigger -- the honest version of the 1m idea); compression ("1"
   before the break) filter; re-entry governor on B; catastrophic price fail-safe (matters once
   leverage > 1); a second instrument / regime window (guard regime flattery -- TVB-1 was ~4mo).

## Open questions carried from the charter (Section 8)
- Chop handling: size-down vs stand-aside when the regime layer is grey -- the two-layer build
  forces this decision; make it deliberately.
- Re-entry governor (cooldown / required new extreme) -- implicated by Control B's 2263 trades.
- Price-stop vs state-stop by holding horizon (still state-stop only so far).

## External Review (protocol active as of 2026-06-30)

Repo now has a PUBLIC remote (`origin`) wired. `/session-end` gate-then-pushes behind
`scripts/secret_scan.py`; each HANDOFF entry carries an `### External Review` block; reviews
(local Codex CLI or cloud) land in `docs/reviews/tvb{N}-codex-audit.md`. At session start, check
`docs/reviews/` for the prior session's review. Full contract: `docs/EXTERNAL_REVIEW_PROTOCOL.md`.

## Reference
- `docs/ATLAS_Timeframe_Continuity_Charter.md` (Section 0 first)
- `CLAUDE.md` (STRICT COMPLIANCE)
- `docs/HANDOFF.md` (TVB-1 entry at top)
- Memory: `tvb1-controlB-fee-churn-finding.md`, `tvb1-tv-mcp-reader-newui.md`
