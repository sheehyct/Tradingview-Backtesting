# Session Startup Prompt -- TVB-1

**Previous Session:** TVB-0 (workspace bootstrap, 2026-06-27)
**Current Branch:** `main`
**Commit state:** Bootstrap scaffold committed locally. No remote yet.
**Plan Mode:** Consider ON for the first real design session (baseline strategy is a
strat-methodology STOP-and-ASK zone).

---

## READ FIRST

This workspace was scaffolded 2026-06-27. The governing design doc is
`docs/ATLAS_Timeframe_Continuity_Charter.md` -- **read its Section 0 before any work.**
The backtest vehicle is TradingView `strategy()` scripts driven via the `tradingview` MCP,
NOT a Python engine. Python here is the analysis layer only.

Nothing has been built yet beyond the scaffold. The first substantive task is to design and
write the **baseline strategy** with the user (charter Sections 3-4) -- this touches STRAT
detection logic, so invoke `strat-methodology` and design before coding.

## NEXT (in order)

1. **Baseline strategy() in Pine (TVB-1, design WITH user).** The control system:
   `{2-trigger (raw level cross, once-per-bar) + TFO FTC-gate + state-stop}` with a fixed,
   a-priori timeframe set. Bake the compound logic into ONE strategy; one "order fills only"
   alert. Audit every `request.security` for `lookahead_off`. See `pine/README.md`.
2. **Wire the analysis layer.** A loader for Strategy-Tester exports -> regime tagging ->
   distribution view. Lives in `analysis/`. Compare every combination to the control.
3. **First exploration sweep.** Run several timeframe-set combinations across distinct regimes
   vs the control. Expect a wide spread; flag surprises. Ablation (compression filter) only
   AFTER the continuity-only baseline is trusted.

## Open questions carried from the charter (NOT decided -- see charter Section 8)

- Chop handling: size-down vs stand-aside when the regime layer is grey (load-bearing).
- Re-entry governor (cooldown / required new extreme).
- Price-stop vs state-stop selection rule by holding horizon.
- Volatility-conditional timeframe sets (parked; build only if threshold + mapping are fixed
  a-priori AND lead/lag is verified empirically -- charter Section 6).

## Reference

- `docs/ATLAS_Timeframe_Continuity_Charter.md` -- the governing charter (read Section 0 first)
- `CLAUDE.md` -- workspace rules (STRICT COMPLIANCE; inherits the spine)
- `docs/guides/TRADINGVIEW_MCP_SETUP.md` -- MCP wiring + gotchas for this workspace
