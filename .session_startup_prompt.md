# Session Startup Prompt -- TVB-4

**Previous Session:** TVB-3 (2026-07-02)
**Current Branch:** `main`
**Commit state:** TVB-3 committed + PUSHED to `origin` (PUBLIC) after secret-scan. Jackson
reader hardening committed SEPARATELY (`tradingview-mcp-jackson` commit `55e93f1`, main) --
deploys at next MCP restart.
**Plan Mode:** ON for the two-layer design (step 3 -- Pine change + charter 3.3 design fork,
strat-methodology STOP-and-ASK). OFF for the mechanical preflights (steps 1-2).
**Prior-session review:** REQUESTED -- check docs/reviews/tvb3-codex-audit.md at session start.

---

## READ FIRST
TVB-3 rebuilt the instrument: the FTFC gate WAS stale (preflight N1 confirmed empirically) and
was fixed via local period-open reconstruction; a TV margin-call deadlock artifact was found and
removed (margin 0/0); the corrected-gate fee sweep REPLACED all TVB-1/2 numbers -- A = +38.6..42%
at real fees (PF ~1.25, DD 16%), B = SIGN-FLIPS inside the real-fee band (-7.4% @ 0.0125% /
+15.2% @ 0.0086%). The controls are now trustworthy; TVB-4 = EXPERIMENTATION PHASE (charter S5),
gated by two cheap preflights. Do NOT quote TVB-1/2 numbers for this strategy.

## NEXT (in order)
1. **Bar-magnifier fidelity check (mechanical, ~15 min):** toggle `use_bar_magnifier`
   (in_32 on the current compiled version -- RE-DERIVE ids first, they shift on any source
   change) on the corrected controls; re-read A and B at 0.0125%. B (knife-edge, PF 0.98-1.03)
   is most exposed -- a sign flip here re-scopes everything downstream. Record delta in the
   datasheet. Requires TV Desktop on CDP 9222 (relaunch flow in memory/HANDOFF).
2. **Kind-window bug-test (charter S1, brittle-on-purpose):** pick an obviously-favorable
   MSTRUSDT trend window; the gate must be unmissably profitable zero-fee there. If NOT,
   that exposes a logic bug BEFORE generating 30 combinations of data with it.
3. **Two-layer design WITH user (PLAN MODE ON, STOP-and-ASK):** HTF regime layer over LTF
   execution layer (e.g. Control-A set governs size/permission; Control-B set generates
   signals) per charter 3.3. Forces the S8 open question: size-down vs stand-aside on regime
   grey. TF sets chosen A-PRIORI with the user, then NOT tuned on the sample.
4. **Experimentation sweep (charter S5):** timeframe-set combinations (both layers) vs the
   corrected control baselines, across regimes; the second regime window folds in here.
   Expect spread; the spread is the finding. Compare everything to controls A/B; flag
   surprises explicitly.

## Deferred (do not lose)
- Slippage REALISM (1 tick/fill is already baked in -- validate vs book depth at ~90-contract
  market-order size before any deployability verdict; all in-session comparisons share the
  cost model so rankings are unaffected).
- Limit-entry (maker) variant: likely moot at cheap taker; a DISTINCT strategy if ever built.
- Realtime gate asymmetry observation: MOOT for the fixed gate (backtest==live by construction).

## Operational notes (hard-won in TVB-3; details in memory `tvb3-stale-gate-margin-artifacts`)
- Strategy on chart: entity `dILiiy` (RE-CHECK id) on OKX:MSTRUSDT.P 15m, Control-B enables,
  in_23=0.1. Slot = "TFC Baseline" (`USER;e7c8...`), byte-identical to
  `pine/baseline_continuity.pine` (Pine v6). Current id map: in_23=commission, in_24=slippage,
  in_25/26=margin, in_32=bar magnifier -- but RE-DERIVE after ANY source change.
- Chart history unloads on TF change: loop `mainSeries().requestMoreData(500)` until exhausted
  (data floor 2026-02-25 10:00Z). VERIFY every recompute took (status probe + dataSize) before
  reading results. Never call `study.restart()` raw via CDP (permanent wedge).
- If TV shows "Session disconnected (accessed from another device)": click Connect -- it is our
  own relaunch; while disconnected, layout/tab ops and restudies silently no-op.
- Watch results for `marginCalls > 0` or a stuck open trade -- the deadlock signature.
- The user's personal chart was left on OKX:MSTRUSDT.P (displaced from CBOE:DRAM 1m).

## Open questions carried from the charter (Section 8)
- Chop handling: size-down vs stand-aside when the regime layer is grey (step 3 FORCES this).
- Re-entry governor (cooldown / required new extreme) -- B's 2801 trades.
- Price-stop vs state-stop by holding horizon (still state-stop only).

## Reference
- `docs/TVB2_control_AB_rerun.md` (canonical datasheet; TVB-3 sections at the end)
- `docs/ATLAS_Timeframe_Continuity_Charter.md` (Section 0 first); `CLAUDE.md` (STRICT COMPLIANCE)
- `docs/HANDOFF.md` (TVB-3 entry at top); `docs/EXTERNAL_REVIEW_PROTOCOL.md`
- Memory: `tvb3-stale-gate-margin-artifacts`, `tvb2-fee-resolved-real-taker`,
  `tvb1-tv-mcp-reader-newui`, `feedback-relaunch-is-cheap`
