# Session Startup Prompt -- TVB-16

**Previous Session:** TVB-15 (2026-07-20; paper twin built + week-1 freeze,
TVB-14 audit folded in same day, v6.1 deployed, score dual-review delivered)
**Current Branch:** `main`
**Commit state:** pushed to `origin` (PUBLIC) through the session-end commits
**Plan Mode:** OFF for the ride-along refresh (mechanical); ON for week-end
analysis and any deferred-audit design work (decide-WITH-user).
**Prior-session review:** REQUESTED -- check docs/reviews/tvb15-codex-audit.md
at startup; fold in critically if returned.

---

## READ FIRST

Week 1 of the paper-trading protocol is RUNNING (window 2026-07-20 00:00 ->
2026-07-27 00:00 UTC; docs/experiments/tvb15_paper_week1_protocol.md is the
contract). The recording instrument is the Python twin (analysis/paper/:
roster frozen at 10 symbols + DRAM parity entry, HL bars archived, replay
deterministic). TFC-BF v6.1 is LIVE on the DRAM 5m (USER;7c28fa0b version
7.0) after the TVB-14 audit's two HIGH findings were confirmed, reproduced,
fixed, and parity-verified same day (alive counts chart==twin 13/11/2/0).
The single most important next move: refresh the archive + replay (the 5m
floor slides ~17 days -- MUST run by 2026-07-23) and collect the user's
exit-class grades against the twin log.

## NEXT (in order)

1. RIDE-ALONG REFRESH: `uv run python -m analysis.paper.archive --roster
   analysis/paper/roster_week1.json` then `... -m analysis.paper.replay
   --roster ...`; present the scoreboard delta; collect user grades;
   number any user-vs-twin or chart-vs-twin divergence TVB15-Dn in the
   protocol doc. Watch the first v6.1-era exits (restored 12h/D rungs are
   now reachable -- TSLA short's -7.4% rung, AAPL break lines).
2. TVB-15 external review: fold in docs/reviews/tvb15-codex-audit.md if
   returned (focus: engine port fidelity, v6.1 fix edges, golden
   methodology). If pending, note and proceed.
3. IF the week has ended (>= Jul-27): week-end pass per the protocol --
   TV-vs-twin parity on DRAM + 2 roster names, exit-class report
   (give-back per class = THE metric), state the heat-conditioning of the
   sample (GPT framing, protocol Section "Fix-forward record"), then the
   deferred audit items as design questions WITH the user: last-only
   supersede scope (duplicate-line class), fixture assertions/interleave,
   min_sep holdout-freeze protocol. Draft the week-2 protocol: candidate =
   full-universe twin (all ~31 floor-passers), roster as attention subset.
4. Scanner thread (hip3-scanner workspace, NOT here): the score
   dual-review was delivered to
   hip3-scanner/docs/SCORE_METHODOLOGY_DUAL_REVIEW_2026-07-20.md
   (UNCOMMITTED there -- its session commits it). TVB side may start
   consuming tf.{1d,1w,1M}.formingDir as the true gate readout any time
   (already served by /api/state; zero scanner deploy).
5. Standing (budget only, LAST): bf_comprehension audit fixes; comparator
   v2; recurring archive automation if manual cadence chafes.

## Open questions carried

- Flip-backstop granularity (full vs partial D+W) -- still OPEN from TVB-14.
- Cross-pool duplicate lines (no dedup); consumption-on-any-touch review
  after the week's data; same-bar entry+touch edge (documented, accepted).
- Audit-deferred: last-only supersede search; fixture two-pass remediation
  (the twin engine + goldens are the parity oracle meanwhile); min_sep
  "provisional example-derived" -> holdout-freeze protocol if it is to
  earn a-priori status.
- Week-2 roster contamination fix: full-universe twin vs heat-conditioned
  labeling (decide at week end, pre-registered).

## Operational notes (hard-won TVB-15)

- Deploy recipe unchanged and proven again (TV launch with CDP, pine-editor
  panel, ~8s Monaco wait, pine_get_source BINDING VERIFY, set_source,
  smart_compile, save, VERSION BUMP check via pine_list_scripts, remove old
  instance, editor Add-to-chart ~540,79). Table title now carries the
  version ("v6.1 pools") = on-chart deploy verification signal.
- Twin engine has v6.0-mode flags (supersede_per_side=False,
  evict_retired_first=False) ONLY for the fixture-parity goldens; the
  replay always runs v6.1 defaults. Golden exceptions:
  FIXTURE_SUPERSEDE_SHADOWS in tests/test_paper_engine.py.
- HL 5m floor ~17 days; replay drops each series' last (forming) bar; the
  archive merge completes it next run.
- Scanner /api/state universe GROWS between snapshots (new listings) --
  intersect before computing drift stats.
- Alerts: none exist on the v6.1 instance yet; any the user creates now
  bind to the current version.

## Reference

- docs/experiments/tvb15_paper_week1_protocol.md (the week's contract +
  fix-forward record)
- docs/HANDOFF.md TVB-15 entry (top) -- the full arc
- docs/reviews/tvb14-codex-audit.md (ADDRESSED same day) +
  tests/test_paper_engine.py regression pins
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first)
- CLAUDE.md (STRICT COMPLIANCE)
