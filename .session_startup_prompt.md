# Session Startup Prompt -- TVB-26

**Previous Session:** TVB-25 (2026-08-16)
**Current Branch:** `main`
**Commit state:** pushed (session range `b31c11d^..` -- concrete head pinned in docs/reviews/REVIEW_REQUEST.md)
**Plan Mode:** OFF unless the user opens a design lane (e.g. the P2
T1-retrace question or any new arm); then ON
**Prior-session review:** REQUESTED -- next session checks docs/reviews/tvb25-codex-audit.md

---

## READ FIRST

TVB-25 did the whole arc in one session: TVB-24 audit folded (F3-F6
fixed + tested; F1/F2 became a user-ruled prereg amendment via live
walkthrough), then the exit round was BUILT AND RUN per the amended
prereg -- fresh harvest (D13 pin Aug 3 -> Aug 16), engine extensions
behind inert defaults, runner with all hardened gates, both windows, all
gates PASS, report + the new plain-language ARM_LEDGER committed. The
single most important next move: fold the TVB-25 review when returned,
then the month-end window extension (after Aug 31).

## NEXT (in order)

1. Fold the TVB-25 review if returned (docs/reviews/tvb25-codex-audit.md;
   the range covers the amendment, engine race, runner gates, and report
   claims). Reproduce before adjudication, as always.
2. Month-end: extend the fresh window through 2026-08-31 24:00 UTC under
   the SAME prereg (harvest first, D13-style pin, rerun; may need ~Sep 1).
3. USER-OWNED design question surfaced by the round (plan mode ON if
   opened): P2 loses per-matched-trade to both D1 and simple P1; the
   T1-retrace middle-dump is the prime suspect. Any refinement is a NEW
   a-priori variant -- never tuned on this sample.
4. TV mirror per arm ON DEMAND with its own parity gate (nothing new is
   TV-valid); the package pine header's "seed-exact" wording fix rides
   the next TV sync.
5. Carried: assessment owner decisions (kernel-vs-Pine, 1m archiving,
   spine CLAUDE.md row); TVB-18 repairs bundle; M+T collaborator PMG+
   nudge; jackson set_inputs fix; tvb8/tvb9 reviews unreturned.

## Open questions carried from the charter (Section 8)

- Anchor clock (UTC vs RTH) and weekend-protocol arms: pre-registered,
  not run.
- S3.1 stands (package arms have never adjudicated it; A1F +23.0 vs A0b
  +104.8 July remains the standing negative).
- Exit-design lane: the round's evidence base now includes the
  occupancy-vs-per-trade decomposition on EVERY contrast (matched
  receipts in analysis/paper/tier_b_exits/) -- read before any new exit
  design.

## Standing practice (new, binding -- CLAUDE.md Reporting rules)

docs/ARM_LEDGER.md translates every arm into plain trading terms and is
updated EVERY round; design sessions restate proposed arms in its
vocabulary (user-confirmed) BEFORE prereg commit; AskUserQuestion options
are phrased in trader language too (or dual trader+code).

## Reference

- docs/experiments/tvb25_exit_round_report.md + docs/ARM_LEDGER.md
- docs/experiments/tvb25_exit_round_prereg.md (incl. 2026-08-16 amendment)
- analysis/paper/tier_b_exits/ (all artifacts incl. matched-entry receipts)
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first)
- CLAUDE.md (STRICT COMPLIANCE)
