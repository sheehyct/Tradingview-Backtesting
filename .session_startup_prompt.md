# Session Startup Prompt -- TVB-5

**Previous Session:** TVB-4 (2026-07-03)
**Current Branch:** `main`
**Commit state:** TVB-4 committed + PUSHED to `origin` (PUBLIC) through `68597d6`
(+ the session-end doc commit). Two-layer regime Pine is the go-forward baseline.
**Plan Mode:** OFF for the sweep mechanics (TF-set combinations are a-priori, then run);
ON only if the sweep design itself needs a fork with the user.
**Prior-session review:** REQUESTED -- check `docs/reviews/tvb4-codex-audit.md` at start
(via `docs/reviews/REVIEW_REQUEST.md`, the reviewer pointer).

---

## READ FIRST
TVB-4 built the two-layer regime architecture (charter 3.3) and ablated it. HEADLINE: at
real fee 0.0125%, the M/W/D regime layer in **stand_aside** mode flips Control-B from
**-8.60% to +40.26%** (PF 1.195, DD halved to 14.3%, Sharpe -0.10 -> +0.75) by cutting 67%
of trades that were disproportionately fee-burners. The charter S8 grey-handling question is
ANSWERED with data: **stand_aside beats size_down** (+40.26 vs +12.84 @0.0125). The
two-layer is now the ablation baseline. TVB-5 = the priority-4 timeframe-set SWEEP.

## NEXT (in order)
1. **Ratify the TVB-4 read (Fable, judgment):** confirm the stand_aside S8 decision and the
   P5 late-entry-tax reading against the datasheet; decide whether stand_aside becomes the
   DEFAULT grey rule baked into the control, or stays an input. Note the size_down KIND-DOWN
   surprise (regime-dependent grey rule; parked vol-conditional avenue -- do NOT tune here).
2. **Fold in the TVB-4 Codex review** if returned (REVIEW_REQUEST.md pointer).
3. **Priority-4 timeframe-set sweep (charter S5):** vary BOTH layers' TF sets a-priori vs
   the two-layer + controls A/B, across regimes; the spread is the finding. Regime-set
   candidates and execution-set candidates chosen a-priori WITH the user, then NOT tuned.
   Second regime window (a non-crash/rally sample) folds in here for regime-flattery guard.
4. **Deferred (carry):** short-leg MAE/solvency check vs 1x cash margin before ANY
   deployability language (Codex TVB-3 finding 2); slippage realism at ~90-contract size.

## Operational notes (hard-won; details in memory `tvb3-stale-gate-margin-artifacts` + this session)
- TV restart RESETS the chart to the personal layout and DROPS the strategy study (it was
  in-memory, not saved to layout). Re-add flow: launch (kill existing first -- a bare launch
  over a running instance does NOT attach the debug port; `Stop-Process -Name TradingView
  -Force` then relaunch with `--remote-debugging-port=9222`), set OKX:MSTRUSDT.P 15m, open
  pine editor, pine_open "TFC Baseline", click Add to chart, RE-DERIVE the entity id (it
  changes every add) + id map. Escape closes the promo modal.
- Current entity was `9lQ6Gn` (WILL change on next re-add). Id map (pineVersion 18):
  exec enables in_2..in_13 (M/W/D/60/30/15 pairs), in_16 reg_mode
  (off|stand_aside|size_down), in_23 grey_size_pct=50, in_24 reg_exit=false, in_32
  commission, in_33 slippage, in_34/35 margins, in_41 bar magnifier.
- Load history to floor before EVERY read: `node scripts/tv_probe.mjs history` (loops
  requestMoreData to the 2026-02-25 10:00Z floor). Verify recompute before reading.
- Run-matrix dump: `node scripts/tv_dump.mjs <out.json>` (metrics + L/S + trade list).
- Sanity gates EVERY run: marginCalls 0, open trades <= 1, list reaches present.
- Chart left on OKX:MSTRUSDT.P 15m, reg_mode=off (control resting state), fee 0.0125.
  User's personal chart (CBOE:DRAM 1m) remains displaced -- restore only if asked.

## Open questions carried from the charter (Section 8)
- Grey handling: **DECIDED this session -- stand_aside** (data: +40.26 vs size_down +12.84
  @0.0125). Fable to ratify; open sub-question = make it the default or keep as input.
- Re-entry governor (cooldown / new-extreme) -- still open; stand_aside implicitly cut churn
  67% but a cooldown was not separately tested.
- Price-stop vs state-stop by horizon -- still state-stop only; a price-stop variant would be
  bar-magnifier SENSITIVE (TVB-4 preflight 1 carve-out), re-check the magnifier if built.
- Vol/trend-conditional grey rule -- NEWLY surfaced by the size_down KIND-DOWN win; parked
  (charter S6), do NOT tune on this sample.

## Reference
- `docs/TVB2_control_AB_rerun.md` (canonical datasheet; TVB-4 two-layer ablation at the end)
- `docs/ATLAS_Timeframe_Continuity_Charter.md` (Section 0 first); `CLAUDE.md` (STRICT COMPLIANCE)
- `docs/HANDOFF.md` (TVB-4 entry at top); `docs/EXTERNAL_REVIEW_PROTOCOL.md`
- `pine/baseline_continuity.pine` (two-layer, Pine v6, slot "TFC Baseline" `USER;e7c8...`)
- `docs/guides/MANUAL_CONTROL_AB_GUIDE.md` (manual cross-ticker backtesting)
- Memory: `tvb3-stale-gate-margin-artifacts`, `tvb2-fee-resolved-real-taker`, this session's
