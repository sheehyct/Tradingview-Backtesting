# Session Startup Prompt -- TVB-3

**Previous Session:** TVB-2 (2026-06-30)
**Current Branch:** `main`
**Commit state:** TVB-2 committed + PUSHED to `origin` (PUBLIC) after secret-scan. Reader fix
committed in `tradingview-mcp-jackson` (SEPARATE repo, commit `fb2a788` on `main`).
**Plan Mode:** ON EARLY -- the amended order below fronts P2a/P2b, and P2b is a Pine change
(strat-methodology STOP-and-ASK). OFF only for the mechanical settings-dump / re-run steps.
**Prior-session review:** RETURNED + FOLDED. Codex audit (`docs/reviews/tvb2-codex-audit.md`; all 4
findings CONFIRMED) verified by the Fable 5 preflight review
(`docs/reviews/tvb3-preflight-fable5-review.md`); the NEXT order below is the preflight's amended
order (user-approved 2026-07-01). Both docs committed.

---

## READ FIRST
TVB-2 fixed the TV MCP reader (clean JSON now) and resolved the fee saga to GROUND TRUTH: the real
xyz taker rate is ~0.0086-0.0125% (HL `crossed` flag, validated against HL's published 0.0432%
crypto taker), ~10x BELOW the 0.1% the backtest used. That REVERSES "B is dead by fees" -- at real
cost A is a clear winner (~+38-42%) and B is near breakeven (~+5-25%). BUT those are GEOMETRIC-MODEL
estimates, and the binding constraint has MOVED to SLIPPAGE (market/taker fills; 2301 round-trips for
B). Single most important next move (AMENDED by the preflight review): verify/fix the HTF-open gate
FIRST -- preflight N1 argues it is near-certainly STALE (prior-period open) AND backtest-vs-live
asymmetric -- THEN re-run A and B at the REAL fee on the corrected gate, THEN model slippage. Fee is
no longer the blocker. Full datasheet: `docs/TVB2_control_AB_rerun.md`.

## NEXT (in order -- amended per `docs/reviews/tvb3-preflight-fable5-review.md` Section 4)
1. **Settings dump first (reproducibility; Codex 1 + preflight N3):** decode ALL inputs/properties
   of the strategy entity (was `35dVw8` -- RE-CHECK via chart_get_state; slot "VIX Spike Alert" /
   title "Baseline TFC [TVB-1]"): commission, SLIPPAGE (script declares `slippage = 1` -- did the
   TVB-2 runs include it or was it overridden to 0?), bar-magnifier, leverage input, and mintick.
   Record into the datasheet. Patch `docs/TVB2_control_AB_rerun.md`: slippage wording (~176-190),
   deferred-checklist fee target 0.0864% -> 0.0125% (Codex 3), annotate superseded deferred items (N4).
2. **P2a: verify HTF-open currency** (overlay strategy FTFC bar-coloring vs `Timeframe Continuity
   [TFO]`, already on chart). Preflight N1 expects STALE (prior-period open for TFs above chart)
   plus a backtest-vs-live repaint asymmetry. If it MATCHES instead, record that, mark N1 wrong,
   and revert to the pre-amendment order (re-runs next).
3. **If stale -- P2b Pine fix (plan mode ON, STOP-and-ASK):** local period-open recon
   `ta.valuewhen(timeframe.change(tf), open, 0)` + trigger tick-offset (`high + syminfo.mintick` /
   `low - syminfo.mintick`) + chart-TF<=enabled-TF guard + stale fee header comment (N5), ONE edit.
   Then ONE bridge run (A @ 0.1%, fixed gate, same window) to quantify the staleness delta vs
   TVB-2's A @ 0.1% = -12.0%. NOTE: input ids (in_2/in_23/...) SHIFT after any Pine source change --
   re-derive them, never reuse cached ids.
4. **Re-run A and B at the real taker fee** (0.0086% modal AND 0.0125% notional-wtd) on the
   corrected gate, replacing the geometric estimates. Control B = enable 60/30/15 + disable M/W/D,
   chart 15m; Control A = enable M/W/D/60 + disable 30/15, chart 60m. Both 1x, both directions,
   same window. Preflight N2 VERIFIED the geometric model against both 0.1% actuals (~0.1pp), so
   expect ballpark agreement (A +38..42%, B +5..25%, adjusted by step 3's gate delta) -- a material
   deviation is itself a finding to chase, not to welcome.
5. **Jackson reader hardening (Codex 4):** propagate `debug_sources` through getStrategyResults/
   getTrades public returns; give getEquity the same debug branch. Separate repo, separate commit.
6. **Sanitized fee artifact (Codex 2):** small `analysis/` script producing counts + RATES by
   {dex, crossed} from a local fills export. Never commit raw fills; no absolute notionals
   (PUBLIC remote).
7. Then as previously ordered: **model slippage** (asymmetric tick offsets; scope informed by step
   1's answer -- incremental if 1 tick was already in), **bar-magnifier fidelity check** (Codex trap
   #2; thin-edge B most exposed), **cherry-picked "obviously-profitable window" bug-test** (charter
   S1), and only AFTER controls are clean at real cost + slippage: two-layer design (charter 3.3) +
   second regime window (TVB-2 was a single ~4mo downtrend -- regime flattery risk).

## Open questions carried from the charter (Section 8)
- Chop handling: size-down vs stand-aside when the regime layer is grey (two-layer build forces it).
- Re-entry governor (cooldown / required new extreme) -- B's 2301 trades.
- Price-stop vs state-stop by holding horizon (still state-stop only).
- NEW: limit-entry (maker) variant -- breakout entry is intrinsically TAKER, so maker is NOT a free
  fee swap; likely moot if the cheap-taker baseline confirms. Treat as a distinct strategy if built.

## Reference
- `docs/reviews/tvb3-preflight-fable5-review.md` (Fable 5 preflight: Codex findings verified, N1
  gate-staleness case, N2 geometric-model verification, amended order rationale)
- `docs/TVB2_control_AB_rerun.md` (control 2x2 + ground-truth fee model; CANONICAL, supersedes the
  Downloads/TVB2_fee_handling.md draft)
- `docs/ATLAS_Timeframe_Continuity_Charter.md` (Section 0 first); `CLAUDE.md` (STRICT COMPLIANCE)
- `docs/HANDOFF.md` (TVB-2 entry at top); `docs/EXTERNAL_REVIEW_PROTOCOL.md`
- Memory: `tvb1-tv-mcp-reader-newui` (reader FIXED + new API ref), `tvb2-fee-resolved-real-taker`,
  `tvb1-controlB-fee-churn-finding`, `feedback-relaunch-is-cheap`
