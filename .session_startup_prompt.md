# Session Startup Prompt -- TVB-22

**Previous Session:** TVB-21 (2026-08-08/09)
**Current Branch:** `main`
**Commit state:** pushed (session range `f90d0c9^..` -- concrete head pinned in docs/reviews/REVIEW_REQUEST.md)
**Plan Mode:** OFF (build session against pinned specs; strat-methodology
skill + STOP-and-ASK still mandatory for any pattern-semantics ambiguity)
**Prior-session review:** REQUESTED -- next session checks docs/reviews/tvb21-codex-audit.md

---

## READ FIRST

Tier B ran and the autopsy is clean: the M+T package arms died to ONE
identified mechanism (trades born beyond their own frozen reclaim target,
created by re-entry-while-signal-persists -- a strategy-ization artifact,
not a pattern defect), the S3.1 contrast came back negative for patterns
under both fill conventions, and the chop veto's fixed 2% band does not
transfer across symbols (47-100% suppression). The single most important
next move is USER-REQUESTED at TVB-21 close: build the TV-side PACKAGE
strategy() so the user can run it live as the drift/bug detector. Build
shape (assessed TVB-21): do NOT strategy-ize the M+T indicator -- insert
its detection/ladder block into the tfc_bf_control_strategy.pine machinery
(the pool engine is required by the BF-prox veto and the C1-style exits) as
a NEW script via the Make-a-copy flow, with arm toggles so one script can
mirror A1 / A2 / A3; compile via MCP; then its OWN parity gate vs the twin
arms BEFORE any drift conclusion. Twin entry events now carry `trig` for
that join.

## NEXT (in order)

1. TV package strategy() port + parity gate (above). Gotchas: TV Desktop
   needs kill-first relaunch with --remote-debugging-port=9222; tab-binding
   trap (Make-a-copy, verify NEW id + unchanged modified stamps); layout
   switch via Open layout... UI + tab_switch.
2. Next-variant pre-registration with the user: T1-floor entry guard (their
   own rule-base component; kills the born-beyond-target class by
   construction) + ATR-scaled vetoes (the chop-veto transfer failure).
   Values pinned a-priori BEFORE any run; still ablations vs C1.
3. Greenlit repairs bundle (TVB-18): F2 roster receipts + fail-closed, F3
   5m-lifecycle warm-up regression, F4 eviction telemetry split,
   freeze-boundary invariant, SKHX tv_symbol/mintick backfill.
4. Fold the TVB-21 review if returned; nudge the M+T collaborator about the
   structurally-dead PMG+ prefix (streak walk seeded at the developing
   bar's own extreme).

## Open questions carried from the charter (Section 8)

- Anchor clock (UTC vs RTH) still a pre-registered-not-run design variable.
- Weekend/low-liquidity protocol arms still pre-registered, not run.
- S3.1: first identified test (A1 vs A0b) came back negative for patterns;
  the thesis stands. Further pattern contrasts only via new pre-registered
  arms.
- HANDOFF.md is over the 1500-line budget (1926 + TVB-21 entry) -- archive
  decision pending user confirmation.

## Reference

- docs/experiments/tvb21_tier_b_report.md (the autopsy; read before the port)
- docs/experiments/tvb21_tier_b_prereg.md (arms + mechanics the port mirrors)
- pine/strat_magnitude_targets_plus.pine + pine/tfc_bf_control_strategy.pine
  (the two sources the port merges)
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first)
- CLAUDE.md (STRICT COMPLIANCE)
