# Session Startup Prompt -- TVB-34

**Previous Session:** TVB-33 (2026-09-04/05): round 2 CLOSED by KILL_FLAT
on the user's word (17:13:32Z, clean 0/0; `data/KILL_FLAT` is the
interlock; funds withdrawn); reject dig; round-3 design session = twelve
dated rulings + D1-D12, prereg'd BEFORE code in both repos; the ledger
replay harness BUILT (hip3-executor `feat/replay-harness`, 1,032 tests);
round-2 parity PASS after three fidelity amendments; nine arms receipted
on both ledgers (weekend 1 watermarked: P5 fails by $0.11 of slippage).
**Current Branch:** `main` (this repo); executor `feat/replay-harness`
pushed, NOT merged
**Commit state:** pushed (both repos)
**Plan Mode:** ON for the three rulings below, then build
**Prior-session review:** REQUESTED (docs/reviews/REVIEW_REQUEST.md,
TVB-33); TVB-31 and TVB-32 still unreturned

---

## READ FIRST

The executor is DOWN by design (KILL_FLAT interlock; never restart it or
rm the interlock without the user). Everything the user needs to rule on
is in docs/ARM_LEDGER.md (Live executor family) and hip3-executor
runs/2026-09-04_replay1/REPLAY.md (dual-language). The replay is a
characterization instrument; nothing in it is a promotion.

## NEXT (in order)

1. THREE RULINGS (plan mode, trader terms, AskUserQuestion):
   a. Weekend-1 P&L gate fails by $0.11, all fill slippage on two thin
      weekend trades (PURR entry 0.9%, STX stop 1.05%). Accept the
      watermarked readings / amend P5 to score net of declared slippage
      cases / declare a fill model a-priori.
   b. The halfway 1-3 (A5) produced ZERO entries under D4's in-force
      check at the cross minute's close (875 candidates: 359 volume, 248
      clock, 268 not beyond the line at the close). Re-time the check to
      the cross instant at the crossing price, or leave the tier
      unreceipted.
   c. Round-3 arm set. Readings: A9 fee-aware floor is the only arm
      positive on both ledgers AND on the matched trades (it removes
      fee-heavy losers: 11 displaced control trades netted -$1.61); A6
      walk-up is negative on both ledgers and on matched trades (-$1.30
      / -$1.94); A4 never bound; A1 admitted one xyz trade (oil/CRCL die
      at the R:R floor once the bell opens); A2/A3/A7/A8 are reshuffles
      whose sign is not stable. Ablation, not tournament: the user picks
      a-priori which changes ride round 3; the receipts inform, never
      decide.
2. Merge `feat/replay-harness` into executor main once the user has read
   REPLAY.md (pure additive: analysis/replay/, tests/replay/, runs/).
3. Scanner PR-B (pivot ladder `tf[id].pivots`, 4h depth 40) then PR-A
   (`1-3h`, label "1-3 (50%SSS)") -- PR-A only after ruling 1b.
4. Executor Ruleset v2 code per the plan's commit sequence; equity
   display fix rides commit 2; config set to the ruled arm set.
5. Round 3: re-fund, deploy scanner then executor, `--once` interlock
   receipt, three supervised probes (D12), rm KILL_FLAT on the user's
   word, tmux `--live`.
6. Carried: month-end fresh-window regen (overdue); TVB-31/32/33 reviews
   when returned; TV mirror on demand; TVB-18 repairs; jackson set_inputs
   fix; metals / tech-vs-yields regime ID (user, later); trade
   visualization still owed; replay `_Context` offline-pure.

## Standing constraints

- Never hardcode the VPS IP (use `<VPS_IP>`); VPS SSH only with explicit
  user confirmation; the master wallet address lives only in the private
  executor repo; `scripts/secret_scan.py` before every push here.
- Every rule change = a dated ruling + prereg BEFORE code; single-change
  arms; the ledger replay receipts an arm before capital.

## Reference

- C:\Strat_Trading_Bot\hip3-executor (branch feat/replay-harness:
  analysis/replay/CONTRACT.md, runs/2026-09-04_replay1/PREREG.md +
  REPLAY.md; README Ruleset v2 block)
- docs/experiments/tvb33_round3_prereg.md (rulings, arms, amendments
  b-h, the two OPEN questions)
- C:\Users\Chris\.claude\plans\glimmering-puzzling-quilt.md (approved plan)
- docs/reviews/REVIEW_REQUEST.md (TVB-33)
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first)
- CLAUDE.md (STRICT COMPLIANCE)
