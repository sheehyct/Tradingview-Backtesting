# Session Startup Prompt -- TVB-13

**Previous Session:** TVB-12 (2026-07-16; audit fold-in + fail-closed replay +
ShortChamp indicator + USER REDIRECT to the exit arc)
**Current Branch:** `main`
**Commit state:** pushed to `origin` (PUBLIC) through the session-end commits
**Plan Mode:** the exit-ablation pre-reg and the compound-3 BF detector are
decide-WITH-user: draft -> explicit approval -> run. Companion upgrade is
mechanical once the metrics are agreed.
**Prior-session review:** REQUESTED, LIGHT SCOPE (user's call: this session was
mostly verification; the TVB-13 implementation gets the full review). Check
docs/reviews/tvb12-codex-audit.md via docs/reviews/REVIEW_REQUEST.md.

---

## READ FIRST -- the mission changed at the end of TVB-12

The user REDIRECTED (memory: project-exit-arc-tvb13): entries are fine --
EXITING WINNERS is the problem. The combined-winner-indicator + regime-label
arc is PARKED. Evidence on file (10 screenshots, read them via the memory
summary if needed):

- FIXTURE (DRAM perp, June, SHORT): entry ~65.88 FTFC-down; -19.87% in the
  trade's favor in 1d6h (TWO daily bars); +25.9% retrace off the ~52.7 bottom
  over ~5d20h; strategy exited 66.66 = -1.2% LOSS on a +20% open winner,
  through ~7 daily pivots + monthly levels taking nothing. Perp gave back more
  than the underlying view. TVB-10 already proved: flip rides regimes whole,
  state churns chop, NEITHER harvests.
- USER'S DEMONSTRATION (compound-3 BF exit, hand-drawn): line = most recent
  OLD scenario-3 candle's low attached to the PREVIOUS candle's low, extended
  forward. Causal, zero lag, known in advance. On the fixture: DAILY line
  harvested ~half the move (-10% break ~18h in); 12H line = 52.79, the June-5
  bottom wick almost to the tick. "Think this one over -- it's almost like
  magic." This IS TVB-11's same_side harvest (which topped the 15m grid) with
  the causal line replacing the lagged 30m/10 fractal line.
- LIVE STAKES: the user has an open DRAM short (July, entry ~72, ~+27% open)
  asking "where do we go from here" -- the exit question is live capital NOW.

## NEXT (in order)

1. Review fold-in if returned (light scope: ShortChamp port fidelity, replay
   comparator method, collector gate, HANDOFF synthesis accuracy).
2. QUANTIFY THE LEAK: add per-episode MFE / MAE / give-back% (realized vs
   peak) to pine/tfc_companion.pine (verified absent 2026-07-16) and run the
   offline give-back distribution from replay/committed trade lists. Hand-label
   the fixture episodes as acceptance tests FIRST.
3. EXIT-ABLATION PRE-REG (draft -> user approval): same verified entries,
   exits race: flip + state + swing-line BF (controls) vs compound-3 BF exit
   (12h/D ladder) vs pivot-ladder trim vs pivot-ladder caution vs ATR/fixed-%
   trails (dumb baselines). DECISIONS NEEDED FROM USER: a-priori TF ladder
   (12h/D/3D/W?); exit at line BREAK vs break-AND-return (close back inside);
   single rung vs trim-across-rungs; fallback when no line is touched (native
   exit as backstop?); line update rule when a new 3 prints. strat-methodology
   skill MANDATORY before the detector Pine; STOP-and-ASK holds.
4. BF design doc: fold the user's answered geometry into
   docs/design/bf_paired_detector_design.md (worktree branch
   worktree-agent-aba898ce6f7f356c9, UNMERGED -- merge after user review).
5. PARKED (do not resume without user): combined winner indicator, regime
   label, HTF viewability, date-range expansion (archiving note below).

## BF comprehension guard (user's closing notes, 2026-07-16 -- READ THIS)

- NEVER analogize broadening formations to Bollinger bands or any envelope
  (Fable slipped into that in visible reasoning at end of TVB-12; user
  flagged it). A BF is two structural lines from ACTUAL bar extremes: lower
  lows connected, higher highs connected. It goes BOTH ways -- connect the
  HIGHS for long-position exits, lows for shorts.
- FRACTALITY (the cheat sheet's "universal truth", copy in
  temporary/tvb13_fixture_screens/a41883e8-9767.jpg): an HTF outside bar IS
  an LTF broadening formation -- every 3 on TF N is a compound 3 of the TFs
  below it. Detection reduces to the most basic STRAT premise: strict lower
  lows / higher highs. Do not overcomplicate.
- Cheat sheet semantics to encode: the BF RE-STARTS when price goes back
  inside a previous range; once inside, mark the RECLAIM levels from the
  prior range extremes; "if we fail the highs we target the lows" = the
  magnitude gauge (fail one side -> attempt the other).
- FIXTURE CORRECTION: the June DRAM episode was NOT cherry-picked -- the
  first short stopped out quickly, the RE-ENTRY caught the -20% move, and
  the daily line (closest 3's low to previous bar's low) breaking was the
  exit signal. Treat n=1 as a measurement limit for the ablation, not as
  doubt about the episode.
- All 11 evidence screenshots preserved in temporary/tvb13_fixture_screens/
  (gitignored; uploads dir is session-scoped and will NOT survive).
- Next session: invoke strat-methodology AND re-read the cheat sheet + the
  BF article text (temporary/) BEFORE any detector design talk.

## Standing sub-threads

- START ARCHIVING HL 5m/15m pulls (TVB-12 never got to it; floor slides daily).
- Doc/label edits from the TVB-11 audit still pending: prereg Section 12
  qualifiers, P3 relabel, request.security rule amendment (F6/F4/F7), NET
  propagation + marginal-trade fixture (F5).
- Weekend-ablation pre-reg; Stage C scalp satellite (deferred carries).

## Operational notes (hard-won)

- TV deploy recipe (memory tv-mcp-pine-tab-binding-trap, SHARPENED): saves
  persist ONLY in the FIRST Pine-dialog session per TV launch; "Add to chart"
  compiles the SAVED version, not the buffer; verify saves by the VERSION BUMP
  in pine_list_scripts. Recipe: restart TV (CDP flag, kill first) -> right-rail
  `pine-dialog-button` (bottom-panel selector is dead) -> Make a copy ->
  pine_set_source -> smart_compile -> verify v2.0 -> Add to chart -> remove old
  study instance. Layout restores resurrect REMOVED studies after TV restarts.
- Artifacts: champion harness = "TVB-EXP Champion [TVB-12r]" study (script
  TVB-12r4, USER;e9873a6d...; scripts 12r/12r2/12r3 are dead intermediates,
  ignorable). Live watch: "Winner: ShortChamp MU5 [TVB-12]" (USER;b70672d9...)
  + the three TVB-11 winners. Collector: scripts/tvb12_replay_collect.mjs
  (fail-closed; nonce echo requires the [TVB-12r] artifact).
- Strategy tables on 24/7 perps NEVER draw on barstate.islast alone (realtime
  bar starvation) -- islastconfirmedhistory + last-bar fallback (in v2 Pine).
- User chart: DRAM 15m layout is the live watch; NASDAQ:MU session pref = 24h.

## Reference

- docs/experiments/tvb12_replay_plan.md -- replay plan + RESULTS (the record)
- docs/HANDOFF.md TVB-12 entry (top); docs/ATLAS_Timeframe_Continuity_Charter.md
  Section 0 FIRST; CLAUDE.md (STRICT)
- Memory: project-exit-arc-tvb13 (THE mission), project-compound3-bf-redesign
  (geometry answered), tvb10-exit-symmetry-regime-bet, feedback-companion-sync-
  cadence, tv-mcp-pine-tab-binding-trap, feedback-results-plain-language
