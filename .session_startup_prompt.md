# Session Startup Prompt -- TVB-30

**Previous Session:** TVB-29 (2026-08-26..28 -- audit fold + round-2 design
+ full implementation)
**Current Branch:** `main`
**Commit state:** pushed (concrete shas pinned in docs/reviews/REVIEW_REQUEST.md)
**Plan Mode:** OFF -- this is a LIVE-OPS session (all rulings are made and
implemented; go-for-it mode applies; the checklist below is the contract)
**Prior-session review:** REQUESTED -- next session checks
docs/reviews/tvb29-codex-audit.md

---

## READ FIRST

TVB-29 folded the TVB-28 audit (all 8 reproduced, zero disputes), ran the
round-2 design session (5 dated rulings + drift-scope amendment
2026-08-28), and LANDED the entire pre-live gate (executor 36d5541, 35
tests) plus scanner cont targets (PR #1). TODAY (Fri 2026-08-28) the user
wants the ROUND-2 LIVE TEST: ~$100 wallet, crypto + xyz equity perps,
from go-live until a manual KILL_FLAT at 18:00 ET (futures close). The
thesis regime (momentum ignition -- indices popping post-Jackson-Hole)
is finally present. Agent wallet is ACTIVE (user confirmed 2026-08-28,
no re-approval needed).

## NEXT (in order) -- the go-live checklist

1. Scanner: user merges PR #1 (github.com/HIP-3-Solutions/hip3-scanner
   pull/1 -- org main is PR-only now); then `railway up --detach` from
   the hip3-scanner checkout + post-deploy verification (/health, payload
   probe showing a cont signal WITH a target, smoke). Cont targets do
   not exist live until this lands.
2. Executor deploy to the ATLAS VPS via deploy/deploy_from_dev.ps1 (new
   git-archive script: refuses dirty trees, records DEPLOYED_SHA). SSH
   touches production -- get the user's explicit go per spine rules.
3. On the VPS: short DRY-RUN first -- verify the decisions journal shows
   the new refusal reasons (counter_drift crypto-only, target_beyond_
   reach, underlying_closed) and cont targets resting in paper brackets;
   run the KILL_FLAT drill flat (expect clean receipt + halt). Then
   `rm data/KILL_FLAT` DELIBERATELY, set live env, start.
4. Supervised probes = the first live trades: watch one bracket land
   (verified resting stop in the journal), verify the equity formula
   against the venue UI with exactly one isolated position open (gate
   item 6's open half), and specifically watch the first xyz fill
   (perp_dexs routing never exercised live).
5. Run until 18:00 ET; user (or session on user's word) creates
   data/KILL_FLAT; verify the zero/zero receipt row. Ledger -> a new
   runs/2026-08-28_friday/ record like weekend-1.
6. Carried: month-end fresh-window regen ~Sep 1 in THIS repo (re-pins
   corrected collision census P2 14/9 PX 21/10 + receipts + fee_sides +
   net algebra); TVB-29 review fold when returned; TV mirror on demand;
   TVB-18 repairs; jackson set_inputs fix; tvb8/tvb9 unreturned.

## Open questions carried from the charter (Section 8)

- Anchor clock + weekend-protocol arms: pre-registered, not run.
- Equity-side drift reference (raised by the 2026-08-28 amendment):
  deliberately NOT chosen -- xyz runs unfiltered; a future a-priori
  design choice if wanted.
- Exit-design lane: runner profiles past T1 remain a future prereg.

## Reference

- C:\Strat_Trading_Bot\hip3-executor (README = Ruleset v0 + v1 prereg +
  gate STATUS; tests/ = the 35-test suite)
- hip3-scanner PR #1 (cont near-bank targets, gates green, parity 0/125)
- docs/reviews/tvb28-codex-audit.md (folded) + REVIEW_REQUEST.md (TVB-29)
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first)
- CLAUDE.md (STRICT COMPLIANCE)
