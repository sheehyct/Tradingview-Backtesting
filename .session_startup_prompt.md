# Session Startup Prompt -- TVB-15

**Previous Session:** TVB-14 (2026-07-19..20; TFC-BF v4 -> v5 -> v6, all
deployed + live-graded same day)
**Current Branch:** `main`
**Commit state:** pushed to `origin` (PUBLIC) through the session-end commits
**Plan Mode:** paper-trading protocol + scanner selection = decide-WITH-user
(design conversation first, then build). Pine deploys are mechanical.
**Prior-session review:** REQUESTED -- check docs/reviews/tvb14-codex-audit.md
at startup; fold in critically if returned.

---

## READ FIRST

TFC-BF [TVB-14] v6.0 is LIVE (TV USER;7c28fa0b, fresh instance on the DRAM
5m front chart). Three exit classes now exist and the WEEK is the grading
window: BF S/L Exit (harvest touch), BF Break L/S Exit (adverse confirmed
close through an alive line), Flip S/L Exit (full-gate backstop). The
engine: rolling compound-3 pools on 12h/D/W/M (the user's rule "what series
of bars is completely outside the set before it", N=1..6 smallest wins),
wick-time anchors, min-anchor-separation junk filter (1.0 periods,
a-priori), direction-relative + asymmetric exit semantics. Everything was
field-driven: v5 fixed the zero-BF-exit report (weekly-only lines were
15-35% away; flips won every race), v6 fixed the CL/WTIOIL long that rode a
crash through a lower line (adverse side needed the BREAK class, not touch).

## NEXT (in order -- user-set 2026-07-20)

1. PAPER-TRADING PROTOCOL for the coming week, designed WITH the user
   before anything is built: what counts as a recorded entry/exit (which
   alert set, which exit classes count), log format + where it lives,
   symbol roster, arm TF per symbol, alert wiring per instance (alerts
   must be RE-CREATED after every script update -- they do not follow
   saves). Keep it a-priori; the week grades the v6 defaults, we do not
   tune mid-week.
2. WHAT GETS TRADED -- involve the scanner: https://hip3-alerts-production.up.railway.app/
   (the hip3-alerts worker; /api/state is the machine surface). Fetch and
   read its schema first. This is the tier-3 concept arriving early as a
   SELECTION filter (real volume/OI floors) and the tier-2 seed (STRAT
   pattern targets live there too). Decide the bridge shape: manual roster
   first, then MCP input-push vs Python-side join for candidates.
3. Ride-along: collect the user's grades of the three exit classes as the
   week produces them; divergences become numbered design questions (the
   exit-ablation pre-reg eventually formalizes them).
4. Standing (only with budget): recurring HL bar archiving for the roster;
   bf_comprehension audit fixes (proximity re-scan + June-3 12h fixture);
   comparator v2. LAST as always.

## Open questions carried

- Cross-pool duplicate lines (same object in two pools draws twice,
  inflates alive counts) -- dedup is future work.
- Flip backstop granularity: the CL long rode because the backstop needs
  FULL D/W/M opposition and the monthly held it open. Partial-flip (D+W)
  variant is an open design question, NOT decided.
- Consumption-on-any-touch (a line touched while flat retires silently):
  review after a week of live data.
- min_sep dialing protocol (dial from live watching, never tuned on
  backtests); first-takeout vs wick-extreme anchor micro-fork (dormant).
- Same-bar entry+touch edge: a line swept on the entry bar consumes
  without an exit (documented, accepted).

## Operational notes (hard-won this session)

- Deploy recipe that works EVERY time: restart TV (Stop-Process
  TradingView; Start-Process (Get-AppxPackage TradingView.Desktop)
  .InstallLocation\TradingView.exe --remote-debugging-port=9222), open
  pine-editor panel, wait ~8s for Monaco, pine_get_source to VERIFY
  binding, set_source, smart_compile, save, VERIFY VERSION BUMP via
  pine_list_scripts, then remove old study instance + click the editor
  "Add to chart" button (~540,79). The floating editor window closes via
  its X (~1884,33). Saves stick only in the first dialog session per
  launch.
- chart_manage_indicator add does NOT reach My-scripts; the editor button
  does. indicator_set_inputs still corrupts indicator() studies -- UI only.
- The user's OTHER layouts (e.g. "DRAM") hold older script versions until
  updated/re-added there.
- analysis/tvb14_bf_pool_fixture.py [12h|D|W] = the acceptance fixture vs
  committed HL bars (tvb14_dram_{1d,1h}_hl.json); TV-vs-HL wick variance
  is cents-level (known TVB-6 class).

## Reference

- docs/HANDOFF.md TVB-14 entry (top) -- the v4->v6 arc + decisions
- pine/tfc_bf_watch.pine header -- the honest deltas list
- Memory: project-live-arc-roadmap (v6 state + ratified rules)
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first)
- CLAUDE.md (STRICT COMPLIANCE)
