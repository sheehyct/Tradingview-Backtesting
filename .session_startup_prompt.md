# Session Startup Prompt -- TVB-19

**Previous Session:** TVB-18 (2026-08-03/04; week-1 close-out + TVB-15 audit
fold-in + design direction; ran on remote control)
**Current Branch:** `main`
**Commit state:** pushed to `origin` (PUBLIC)
**Plan Mode:** ON for the design session (STOP-and-ASK zone throughout);
OFF only for the read-only RTH-clock census if it runs first.
**Prior-session review:** TVB-18 REQUESTED -- check
`docs/reviews/tvb18-codex-audit.md` at startup. (TVB-15 RETURNED and folded
in 2026-08-04; tvb8/tvb9 remain unreturned, standing note.)

---

## READ FIRST

Week 1 is CLOSED and ADJUDICATED: no official number (audit F1 selection
lookahead -- it was a process test run; full record and freeze_slice both
committed as documentation only). The user greenlit repairs F2/F3/F4 + a
freeze-boundary invariant, decided NO week-2 rerun, and set the next move:
the EXIT-DESIGN SESSION. Everything it needs is staged -- read
`docs/experiments/tvb15_paper_week1_protocol.md` sections "Week-1
adjudication" and the design-direction bullet, then grep the (gitignored,
local-only) `docs/thestrat_ai/` corpus. The strat-methodology skill is
mid-rebuild: Ambiguity Policy extra-strict, design WITH the user, no
flip-semantics code before agreement.

## NEXT (in order)

1. DESIGN SESSION with the user (plan mode ON). Scope: (a) flip-backstop
   redesign -- drop-the-month is agreed AS A START, but the mechanism must
   count INDEPENDENT opens against the position (uncoupling-aware: on the
   week's first day a close-vs-open D flip and W flip are the same fact;
   see corpus 03/07) and must resolve WHICH flip the backstop means
   (close-vs-period-OPEN gates, the deployed mechanic, vs trades through
   prior-period EXTREMES, the STRAT scenario flip); (b) the open-air stop
   question (does the target ladder replace or complement a hard stop);
   (c) Magnitude+Targets [Custom] integration -- target ladder = harvest
   path (5-6 rungs today, right count TBD), BF = exhaustion overlay at the
   extended rungs, kill-signal there = continuity turning against the
   position while extended. Corpus vocabulary trap: its "flip" = the new
   60-minute open (03/06), NOT our flip backstop.
2. BUILD the greenlit repairs bundle (user decision 2026-08-04, all of
   them, not choose-one): F2 roster receipts + fail-closed guards +
   frozen-source reproduction test; F3 5m lifecycle warm-up (coarse bars
   only for base candles) or a per-symbol common-window state-diff
   regression; F4 eviction telemetry split (events vs alive-sides) + a
   two-alive-side regression; freeze-boundary invariant for FUTURE rosters
   (week-1 files stay frozen). Engine/tests land anytime; the Pine side of
   F4 deploys WITH the design bundle (no live chart mounts the indicator,
   so repo-first carries zero live drift).
3. RTH-vs-UTC CLOCK CENSUS (user question, seeded 2026-08-04, read-only,
   cheap, no strategy code): on the archived bars, per symbol, how often
   do gate/flip signals computed on the venue clock (00:00 UTC rolls --
   current behavior) disagree with the same signals computed on
   RTH-anchored opens (9:30 ET day / Monday week / first-trading-day
   month), and in which hours do disagreements cluster? If they rarely
   disagree, the question settles cheaply; if often, full backtest arms
   (UTC-anchored vs RTH-anchored vs RTH-gated evaluation) become a
   pre-registered experiment. The standing underlying-RTH mirror rule is
   the outside reference arm. This is ALSO a design-session input: the
   anchor clock is a tested variable, not an assumption.
4. TVB-18 external review: fold in `docs/reviews/tvb18-codex-audit.md` if
   returned; else note and proceed.
5. Only after 1-2 land: define the next graded run ("week 2" is NOT a
   rerun of the same config -- it grades the new design, with receipts,
   invariant, and repairs in place; full-universe-vs-roster still open).

## Open questions carried from the charter (Section 8)

- Flip-backstop granularity -- now refined to: independent-opens counting
  (uncoupling-aware) + gate-flip vs scenario-flip semantics + open-air
  stop. THE design-session core.
- Anchor clock (UTC vs RTH) as a tested variable -- new, priority 3.
- Week-2 shape: full-universe twin vs selected roster; what the graded
  artifact is under the target-ladder design.
- Deferred audit items now consolidated: last-only supersede scope,
  fixture assertion/interleave, min_sep holdout-freeze (TVB-14/15), plus
  the greenlit F2/F3/F4 + invariant (priority 2 above).

## Reference

- `docs/experiments/tvb15_paper_week1_protocol.md` (week-1 adjudication +
  audit fold-in + design direction -- the session's contract)
- `docs/thestrat_ai/` (LOCAL-ONLY corpus; README first: grep-first
  discipline, source precedence, OBSERVED-vs-DERIVED figure blocks;
  term-index at reports/term-index.csv; uncoupling = 03/07)
- `docs/reviews/tvb15-codex-audit.md` (the folded audit; F1-F4)
- `analysis/paper/freeze_slice.py`, `analysis/paper/parity_state.py`
- `pine/strat_magnitude_targets_plus.pine` + its README (the addition)
- `docs/ATLAS_Timeframe_Continuity_Charter.md` (Section 0 first)
- `CLAUDE.md` (STRICT COMPLIANCE)
