# Session Startup Prompt -- TVB-10

**Previous Session:** TVB-9 (2026-07-07)
**Current Branch:** `main`
**Commit state:** TVB-9 committed + PUSHED to `origin` (PUBLIC) through the
session-end commits. No sibling-repo changes this session.
**Plan Mode:** OFF for mechanical work. The long-only ablation is a
decide-WITH-user step (draft pre-registration -> explicit approval -> run),
same flow as the TVB-9 breadth pre-registration.
**Prior-session review:** REQUESTED -- check `docs/reviews/tvb9-codex-audit.md`
AND the still-unreturned `docs/reviews/tvb8-codex-audit.md` at start (via
`docs/reviews/REVIEW_REQUEST.md`, the pointer -- it now names the TVB-9 range;
the TVB-8 range is recorded in its HANDOFF block and remains unreviewed).

---

## READ FIRST
TVB-9 completed the ENTIRE Phase-5 arc in one session: venue-bar drift pilot
(band -0.5..-1.3pp; |net|<1.5pp sign-indeterminate, gov 2.5pp), user-approved
pre-registration, the 144-run breadth sweep (regime map: edge only in
high-vol trend regimes; <40% vol dead), mechanism digs, the HTF-index
exploration (dead zone is SIGNAL-structural -- H2 refuted), and the
leverage-extreme overlay (sample-Kelly ~ L_surv/2 on edge cells, venue max =
death). The single most important next move: **fold in returned Codex
reviews, then draft the LONG-ONLY ablation pre-registration with the user**
-- short-side weakness was observed 3x independently (MU 60m shorts -71
log-pp; BTC daily shorts -48.4; CRCL daily -46 overall) and is now the
best-motivated candidate on the board.

## NEXT (in order)
1. **Review fold-in**: tvb9-codex-audit.md (and tvb8 if returned) -- read
   critically, synthesize into HANDOFF, action CRITICAL/HIGH first.
2. **Long-only ablation pre-registration (decide WITH user)**: cells fixed
   from the record (60m grid +/- allow_short=false; possibly MWD_on240),
   expectations written BEFORE runs (prediction from the digs: long-only
   rescues MU-class burst trends, costs little on MSTR/AMD where shorts
   scratch; it is NOT free on down-drift windows -- MSTR b&h was -45.6% and
   shorts carried real weight there). Approval, then run via the existing
   runners.
3. **Deferred carries**: weekday-RTH L2 re-sample (needs a weekday
   14:30-20:00Z window -- mechanical, closes the operative slippage point
   estimate); Pine governor-comment fix bundles with the next Pine-touching
   deployment (+ prefix regression); net-arming ablation = optional
   pre-registered candidate.

## Open questions carried from the charter (Section 8)
- Price-stop vs state-stop by horizon -- still open. NEW data point (TVB-9
  HTF): the close-based state-stop's give-back scales with bar size
  (MWD_onD << MWD_on240 nearly everywhere) -- a price-stop variant at HTF
  would need bar-magnifier re-checks before comparison.
- Vol/trend-conditional grey rule -- still PARKED (charter S6 bar unmet).
- Re-entry governor: KEPT; TVB-9 added the first independent-symbol
  corroboration (CRCL +11.75pp, continuous-whipsaw form).

## Operational notes (hard-won)
- **TV Desktop is RUNNING** (launched TVB-9 via the Store-app command with
  CDP 9222); chart restored to resting state HIP3XYZ:MSTRUSDC.P 15m; NO
  Pine/strategy-slot changes this session (pineVersion 20 anchor intact).
- VBT Pro v2026.6.27 in .venv only; `uv sync` STRIPS it (use
  `uv run --no-sync`); reinstall guide `vectorbt_pro_install.md` (gitignored).
- HL 1h cap ~5,000 candles and it SLIDES daily -- the committed
  tvb9_hl_*_1h.json pulls are irreproducible evidence; reruns of
  scripts/tfc_breadth_sweep.py and tfc_htf_sweep.py read the committed
  files (idempotent by design). Fresh symbols need a fresh fetch + commit.
- Per-symbol conventions: mintick from TV symbolInfo via CDP
  (tvb9_symbolinfo.json: XYZ100 0.01, BTC 0.1, rest 0.001); qty_step =
  10^-szDecimals from HL meta. Never assume.
- The gate is the contract: tests/test_tfc_equivalence_gate.py 8/8 green
  through ANY simulator change. The leverage overlay deliberately
  post-processes trade lists instead of touching the simulator.

## Reference
- `docs/TVB2_control_AB_rerun.md` -- TVB-9 sections (drift pilot,
  pre-registration, breadth results, digs, HTF pre-reg+results, leverage
  overlay) = the record
- `docs/VBT_BREADTH_PORT_PLAN.md` (design of record, Phases 0-5 complete)
- `docs/ATLAS_Timeframe_Continuity_Charter.md` (Section 0 first); `CLAUDE.md`
  (STRICT COMPLIANCE); `docs/HANDOFF.md` (TVB-9 entry at top)
- Memory: `tvb9-breadth-regime-map`, `project-vbt-breadth-engine`,
  `user-leverage-philosophy`, `project-one-layer-screener-attach`
