# Session Startup Prompt -- TVB-20

**Previous Session:** TVB-19 (2026-08-05; overnight autonomous run --
pre-reg + Tier A sweep + clock census + deep TV harvest -- then the
morning visualization discussion and the strategy-port decision)
**Current Branch:** `main`
**Commit state:** pushed to `origin` (PUBLIC)
**Plan Mode:** ON for the design session (STOP-and-ASK throughout);
recommended ON for the strategy-port convention-pinning step too, OFF
for the mechanical Pine fork/parity runs once conventions are agreed.
**Prior-session review:** TVB-19 REQUESTED -- check
`docs/reviews/tvb19-codex-audit.md` at startup. TVB-18 was still
UNRETURNED at TVB-19 close (tvb8/tvb9 also outstanding, standing note).

---

## READ FIRST

TVB-19's overnight run landed three inputs the next work builds on: the
clock census is MATERIAL (31% gate disagreement UTC-vs-RTH -- the anchor
clock is now a design variable), the 864-cell Tier A sweep quantified
the backstop cost and showed the whole top of the table is the
no-backstop corner (the adverse-runner question inverted; labels apply,
nothing promoted), and TV's 5m depth reaches 2026-05-25 (~4x the HL
floor). The user then DECIDED (2026-08-05 morning): visualization =
TradingView-native, NOT a custom explorer -- fork the deployed v6.1
watch indicator into a CONTROL strategy(), parity-gate it against the
Python twin, and leave the Strategy Tester mounted for browsing. That
port is priority 1; it becomes the A/B baseline the exit-design
variants get tested against.

## NEXT (in order)

1. v6.1 CONTROL strategy() PORT + PARITY GATE (user decision
   2026-08-05): fork pine/tfc_bf_watch.pine v6.1 into a strategy()
   with ZERO semantic change -- same pools/lifecycle/exit-race/arm
   clock; strategy.entry/close replaces label+alert emission; exit
   comments label kind + pool TF + N ("BF 12h N=2" / "Break D" /
   "Flip"). Pinned conventions BEFORE running: commission 0, margin
   0/0 (TVB-3), fill model vs the twin's (twin fills BF at line value
   intrabar, entries at trigger +/- 1 tick) -- decide bar-magnifier
   on/off and declare; deep-load history to the floor (scripts/
   tv_probe.mjs history) BEFORE reading any result (warm-up depth
   changes lifecycle state -- audit F3). Parity: TV trade list
   (data_get_trades) vs twin events, deployed-defaults cell, window
   07-06..08-03 on the 5m chart (TV 5m depth covers it fully);
   expected residual = TV-vs-HL wick-level feed deltas (TVB-6
   97-99% float-exact) -- declare, do not hide. Mount on a scratch
   layout (TVB18-parity or new), leave the tester loaded for the
   user to browse. Tab-binding trap applies to script creation
   (Make-a-copy UI flow, verify NEW script id). strat-methodology
   skill mandatory; treat as a parity-proven port of FROZEN deployed
   semantics -- any semantic question = STOP and ASK.
2. EXIT-DESIGN SESSION with the user (plan mode ON; carried from
   TVB-19 unchanged, now with more inputs): (a) flip-backstop
   redesign -- independent-opens counting (uncoupling-aware, corpus
   03/07) + gate-flip (close-vs-open, deployed) vs scenario-flip
   (prior-extreme break, STRAT) semantics; (b) the open-air stop
   question -- NOW QUANTIFIED by the sweep (brk costs ~47pp / flip
   ~35pp median in the kind window; no-backstop corner rides 37-40%
   MAE episodes; same object as the week-1 adverse-runner gap);
   (c) Magnitude+Targets ladder integration (harvest path, rung
   count TBD, BF = exhaustion overlay); (d) NEW: the anchor clock as
   a tested variable (census 31% MATERIAL, day-leg structural,
   mostly different flip events; pre-registered arms justified).
   Design variants get built against the priority-1 control
   strategy. NO flip-semantics code before agreement (skill
   mid-rebuild, Ambiguity Policy strict).
3. BUILD the greenlit repairs bundle (unchanged from TVB-19, still
   pending): F2 roster receipts + fail-closed guards; F3 5m
   lifecycle warm-up or state-diff regression; F4 eviction telemetry
   split + two-alive-side regression; freeze-boundary invariant for
   FUTURE rosters (week-1 files stay frozen). Include the SKHX
   fix-forward: future rosters backfill tv_symbol + re-pull ticks
   from TV metadata (xyz:SKHX = HIP3XYZ:SKHYNIXUSDC.P, true mintick
   0.001 vs hl_inferred 0.1; TV search does NOT index HIP3XYZ --
   direct chart load only).
4. Reviews: fold in tvb19-codex-audit.md if returned; TVB-18 still
   pending.
5. Only after 1-2: Tier B (target-ladder + partial-flip variants,
   design-gated, pre-registered) and the next graded run definition
   (NOT a week-2 rerun). Trace-mode twin instrumentation (gate opens
   + arm extremes + line anchors on events; separate emission path,
   frozen week-1 artifacts stay byte-identical) is deferred until
   the design session needs the forensics.

## Open questions carried from the charter (Section 8)

- Flip-backstop granularity: independent-opens counting + gate-vs-
  scenario flip semantics + open-air stop -- the design-session core,
  now with sweep-quantified stakes.
- Anchor clock (UTC vs RTH) -- census answered "material"; the
  full pre-registered arms (UTC vs RTH-anchored vs RTH-gated) are
  now justified but undesigned.
- Week-2 shape: full-universe twin vs roster; graded artifact under
  the target-ladder design.
- Deferred audit items: last-only supersede scope, fixture
  assertion/interleave, min_sep holdout-freeze (TVB-14/15), plus
  the F2/F3/F4 + invariant bundle (priority 3).

## Reference

- docs/experiments/tvb19_overnight_report.md (the overnight findings:
  census 31%, sweep spread + backstop cost, TV depth + SKHX mapping)
- docs/experiments/tvb19_tier_a_prereg.md + analysis/paper/sweeps/
  tvb19_tier_a/ (the labeled sweep artifacts)
- analysis/reference/tvb19_clock_census.json + analysis/clock_census.py
- analysis/reference/tv_deep/ + README (deep TV bars, provenance,
  SKHYNIX mapping)
- pine/tfc_bf_watch.pine (v6.1 -- the port source; USER;7c28fa0b on TV)
- analysis/paper/engine.py + replay.py (the twin the port must match)
- docs/experiments/tvb15_paper_week1_protocol.md (adjudication +
  design direction)
- docs/ATLAS_Timeframe_Continuity_Charter.md (Section 0 first)
- CLAUDE.md (STRICT COMPLIANCE)
