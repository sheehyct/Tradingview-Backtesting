# Session Startup Prompt -- TVB-9

**Previous Session:** TVB-8 (2026-07-06/07)
**Current Branch:** `main`
**Commit state:** TVB-8 committed + PUSHED to `origin` (PUBLIC) through the
session-end commit. No sibling-repo changes this session.
**Plan Mode:** OFF for the Phase-5 build (the port plan + TVB-8 directives
govern) -- BUT the breadth pre-registration text is a decide-WITH-user step:
draft it, get explicit approval, THEN run. Model handover note: TVB-8 was the
last Fable 5 session; the judgment-heavy design work (Pine semantics port,
calibration corrections, adjudications) is DONE and gate-locked -- Phase 5 is
disciplined execution against a green gate.
**Prior-session review:** REQUESTED -- check `docs/reviews/tvb8-codex-audit.md`
at start (via `docs/reviews/REVIEW_REQUEST.md`, the pointer).

---

## READ FIRST
THE PHASE-3 EQUIVALENCE GATE IS GREEN: `tfc/` reproduces the TV Strategy
Tester trade-for-trade on all 8 reference cells (20,429 trades; datasheet
TVB-8 section = the record). Phases 0-4 are complete (simulator, resampler,
HL/OKX providers -- all live-verified). Python breadth numbers are authorized
BEHIND `tests/test_tfc_equivalence_gate.py` (8/8 must stay green through any
simulator change). The single most important next move: **Phase 5 -- the
MSTR-on-HL-bars pilot, then the pre-registered regime-mapping breadth pass.**

## USER DIRECTIVES for the breadth pass (2026-07-07, binding)
- **NO keep/kill verdicts off the new tickers** -- listings are too young.
  The deliverable is a ROUGH MAP of what regime this system could be used in,
  if at all. Interpret per regime; expect a spread; flag surprises.
- **DRAM is SKIPPED** (listed 2026-05-04, ~2 months; M/W/D regime seeds
  June-1 -- sample too thin even for a vignette). Revisit when it has history.
- **Universe widening is WELCOME**: beyond crypto-related and indices, add
  other xyz equity perps if time/ability allows -- discover what is listed,
  pick a-priori (listing depth / liquidity), NEVER by backtest performance.

## NEXT (in order)
1. **Phase 5a -- xyz-MSTR-on-HL-bars pilot** (plan): fetch HL candles via
   `tfc.providers`, run the a-priori cells, compare economics to the gate
   cells on TV bars. This calibrates venue-bar drift BEFORE any new-symbol
   number is read. Document the drift band in the datasheet.
2. **Pre-registration draft -> user approval**: universe {xyz:MSTR anchor,
   xyz:XYZ100, xyz:SP500, BTC dead-control, + any a-priori equity-perp adds},
   cells fixed from the record (ctrlA M/W/D/60; R1E3 240/120/60 + M/W/D
   stand_aside; gov2 on/off), 60m bars from HL 1h candles, fees {0, 0.0125},
   slip {1, 10} in PER-SYMBOL ticks (fetch each symbol's mintick -- xyz MSTR
   is 0.001; do NOT assume), expectations written BEFORE runs (SP500 = low-vol
   null where the edge SHOULD fail; XYZ100 between BTC and MSTR; containment
   holds everywhere).
3. **Phase 5b -- breadth runner + sweep CLI** (plan): per-run JSON artifacts
   with provider meta (floor_hit, served window) so short histories cannot
   masquerade; metrics from trades/equity directly; optional VBT from_orders
   wrap LAST behind the 5-step MCP workflow.
4. Deferred carries: weekday-RTH L2 re-sample (needs a weekday 14:30-20:00Z
   window; mechanical); Pine governor-comment fix bundles with the next
   Pine-touching deployment (+ prefix regression); net-arming ablation =
   optional pre-registered candidate.

## Open questions carried from the charter (Section 8)
- Price-stop vs state-stop by horizon -- still open (bar-magnifier re-check
  required if a price-stop variant is built).
- Vol/trend-conditional grey rule -- still PARKED (charter S6 bar unmet).
- Re-entry governor: KEPT (cross-venue-verified); arming is GROSS as deployed.

## Operational notes (hard-won)
- **VBT Pro v2026.6.27 lives in .venv only** (NOT pyproject; public repo).
  `uv sync` STRIPS it; reinstall command + auth notes in
  `vectorbt_pro_install.md` (repo root, GITIGNORED -- never commit it).
  numpy is capped <2.5 (numba import ceiling). vectorbtpro import is OPTIONAL
  everywhere (Phase-5 tearsheet wrap only, behind importorskip).
- Live provider tests are opt-in: `TFC_NETWORK=1` env var (network marker).
  HL candle caps: 15m ~52d back, 1h ~5,000 candles (Dec-10 floor for MSTR/
  XYZ100), 1d = since listing. Probe results 2026-07-07: XYZ100 since
  2025-10-13, SP500 since 2026-03-18, DRAM since 2026-05-04.
- `pp` in dumps = pv/(q*ep*(1+comm)) -- cost-basis return, NOT equity return
  (TVB-8 correction); equity evolution must chain pv.
- TV Desktop (Store app) launch: kill any running instance, then
  `Start-Process (Join-Path (Get-AppxPackage TradingView.Desktop).InstallLocation
  'TradingView.exe') -ArgumentList '--remote-debugging-port=9222'`. Chart
  resting state: HIP3XYZ:MSTRUSDC.P 15m, ctrlB defaults, reg/gov off, fee
  0.0125, s1 (untouched this session -- no TV work happened in TVB-8).

## Reference
- `docs/VBT_BREADTH_PORT_PLAN.md` (design of record, TVB-8 amendments inline)
- `docs/TVB2_control_AB_rerun.md` -- TVB-8 section (gate record, pp
  correction, epsilon-floor adjudication, Phase-4 verification)
- `tfc/` package + `scripts/tfc_gate_report.py` + `scripts/tfc_qty_calibration.py`
- `docs/ATLAS_Timeframe_Continuity_Charter.md` (Section 0 first); `CLAUDE.md`
  (STRICT COMPLIANCE); `docs/HANDOFF.md` (TVB-8 entry at top)
- Memory: `project-vbt-breadth-engine`, `tvb7-crossvenue-costs-portplan`,
  `project-one-layer-screener-attach`
